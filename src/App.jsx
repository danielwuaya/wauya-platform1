import React, { useState, useEffect, useCallback, useRef, useMemo, Component } from "react";
import { supabase } from "./supabase.js";
import DriveFiles from "./DriveFiles.jsx";
import KPIDashboard from "./KPIDashboard.jsx";
import Approvals from "./Approvals.jsx";
import ContentCalendar from "./ContentCalendar.jsx";
import Pipeline from "./Pipeline.jsx";
import generateQBR from "./QBRReport.jsx";
import Expenses from "./Expenses.jsx";

// Error Boundary - catches ANY error and shows it instead of black screen
class ErrorBoundary extends Component{constructor(p){super(p);this.state={err:null}}static getDerivedStateFromError(e){return{err:e}}render(){if(this.state.err)return React.createElement("div",{style:{fontFamily:"'Poppins',sans-serif",background:"#060B18",color:"#F0F0F4",minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",padding:20}},React.createElement("div",{style:{textAlign:"center",maxWidth:500}},React.createElement("div",{style:{fontSize:48,marginBottom:16}},"⚠️"),React.createElement("h2",{style:{fontSize:20,marginBottom:8,color:"#F8BA10"}},"Wauya Platform - Error"),React.createElement("p",{style:{fontSize:13,color:"#8A94A8",marginBottom:16}},String(this.state.err.message||this.state.err)),React.createElement("button",{onClick:()=>{localStorage.clear();window.location.reload()},style:{background:"#F8BA10",color:"#060B18",border:"none",borderRadius:8,padding:"10px 20px",fontSize:13,fontWeight:700,cursor:"pointer"}},"Reiniciar plataforma")));return this.props.children}}

// Toast notification system
function Toast({message,type="success"}){const colors={success:C.g,error:C.r,info:C.acc};const icons={success:"✓",error:"✕",info:"ℹ"};const color=colors[type]||C.acc;return<div style={{position:"fixed",bottom:24,right:24,background:`linear-gradient(145deg,${C.s},${C.bg})`,border:`1px solid ${color}40`,borderRadius:12,padding:"10px 18px",display:"flex",alignItems:"center",gap:8,zIndex:2000,animation:"fadeUp .3s ease",boxShadow:`0 8px 25px rgba(0,0,0,.4),0 0 12px ${color}15`,backdropFilter:"blur(12px)"}}><span style={{width:20,height:20,borderRadius:6,background:color+"20",display:"flex",alignItems:"center",justifyContent:"center",fontSize:11,fontWeight:700,color}}>{icons[type]}</span><span style={{fontSize:12,fontWeight:500,color:C.tx,fontFamily:F}}>{message}</span></div>}

// Skeleton loader
function Skeleton({w="100%",h=16,r=6}){return<div style={{width:w,height:h,borderRadius:r,background:`linear-gradient(90deg,${C.b}40,${C.b}80,${C.b}40)`,backgroundSize:"200% 100%",animation:"shimmer 1.5s infinite"}}/>}
function SkeletonCard(){return<div style={{background:`linear-gradient(145deg,${C.s},${C.s}dd)`,borderRadius:16,border:`1px solid ${C.b}`,padding:18}}><div style={{display:"flex",justifyContent:"space-between",marginBottom:12}}><Skeleton w={120} h={14}/><Skeleton w={60} h={14}/></div><Skeleton w="80%" h={10} /><div style={{marginTop:8}}><Skeleton w="50%" h={10}/></div></div>}

// Debounce hook for auto-save
function useDebounce(fn,delay=800){const timer=useRef(null);return useCallback((...args)=>{if(timer.current)clearTimeout(timer.current);timer.current=setTimeout(()=>fn(...args),delay)},[fn,delay])}

const STATUS_OPTS=[{value:"briefing",label:"Briefing",color:"#6B7280",icon:"📋"},{value:"en_progreso",label:"En Progreso",color:"#F59E0B",icon:"⚡"},{value:"revision",label:"En Revisión",color:"#8B5CF6",icon:"🔍"},{value:"aprobado",label:"Aprobado",color:"#10B981",icon:"✅"},{value:"entregado",label:"Entregado",color:"#3B82F6",icon:"📦"}];
const DEL_TYPES=[{value:"imagen",label:"Imagen",icon:"🖼️"},{value:"video",label:"Video",icon:"🎬"},{value:"manual_marca",label:"Manual de Marca",icon:"📖"},{value:"presentacion",label:"Presentación",icon:"📊"},{value:"documento",label:"Documento",icon:"📄"},{value:"otro",label:"Otro",icon:"📎"}];
const PROSP_TYPES=[{value:"propuesta_visual",label:"Propuesta Visual",icon:"🎨"},{value:"propuesta_economica",label:"Propuesta Económica",icon:"💰"},{value:"pagina_web",label:"Página Web",icon:"🌐"},{value:"otro",label:"Otro",icon:"📎"}];
const PRIOS=[{value:"baja",label:"Baja",color:"#6B7280"},{value:"media",label:"Media",color:"#F59E0B"},{value:"alta",label:"Alta",color:"#EF4444"},{value:"urgente",label:"Urgente",color:"#DC2626"}];
const TSTAT=[{value:"pendiente",label:"Pendiente",color:"#6B7280"},{value:"en_progreso",label:"En Progreso",color:"#F59E0B"},{value:"completado",label:"Completado",color:"#10B981"}];
const CATS=[{value:"cliente",label:"Cliente",color:"#60A5FA",icon:"💼"},{value:"propuesta",label:"Propuesta",color:"#F59E0B",icon:"📄"},{value:"interno",label:"Interno",color:"#A78BFA",icon:"🏠"}];
const PROJ_TYPES=[{value:"",label:"— Sin tipo —"},{value:"propuesta_visual",label:"Propuesta Visual"},{value:"website",label:"Website"},{value:"branding",label:"Branding"},{value:"catalogo",label:"Catálogo"},{value:"social_media",label:"Social Media"},{value:"video",label:"Video/Producción"},{value:"otro",label:"Otro"}];
const BILL_STATUS=[{value:"activo",label:"Activo",color:"#34D399"},{value:"por_cobrar",label:"Por Cobrar",color:"#F59E0B"},{value:"vencido",label:"Vencido",color:"#FF4D6A"},{value:"pausado",label:"Pausado",color:"#6B7280"}];
const todayStr=()=>new Date().toISOString().split("T")[0];
const F="'Poppins',sans-serif",D="'Playfair Display',serif";
const C={bg:"#060B18",s:"#0A1428",s2:"#0F1D38",b:"#1A2D52",tx:"#F0F0F4",tm:"#8A94A8",td:"#4A5568",acc:"#F8BA10",accD:"#D4A00E",r:"#FF4D6A",rBg:"#1A0810",g:"#36DE67",w:"#FFC107",p:"#4A90D9",pBg:"#0A1428",bl:"#60A5FA",blBg:"#0A1633"};
const CSS=`@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800&family=Poppins:wght@300;400;500;600;700&display=swap');@keyframes fadeUp{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:translateY(0)}}@keyframes fadeIn{from{opacity:0}to{opacity:1}}@keyframes pulse{0%,100%{opacity:1}50%{opacity:.5}}@keyframes modalIn{from{opacity:0;transform:scale(.96) translateY(8px)}to{opacity:1;transform:scale(1) translateY(0)}}@keyframes shimmer{0%{background-position:-200% 0}100%{background-position:200% 0}}*{box-sizing:border-box;margin:0;padding:0}body{font-family:${F};background:${C.bg};color:${C.tx}}::-webkit-scrollbar{width:5px}::-webkit-scrollbar-track{background:transparent}::-webkit-scrollbar-thumb{background:${C.b};border-radius:3px}::-webkit-scrollbar-thumb:hover{background:${C.acc}40}select option{background:${C.s};color:${C.tx}}input:focus,select:focus,textarea:focus{border-color:${C.acc}!important;box-shadow:0 0 0 3px ${C.acc}12}`;
function extractFolderId(u){if(!u)return null;const m=u.match(/folders\/([a-zA-Z0-9_-]+)/);return m?m[1]:(/^[a-zA-Z0-9_-]{20,}$/.test(u)?u:null)}
function useIsMobile(){const[m,s]=useState(typeof window!=="undefined"&&window.innerWidth<768);useEffect(()=>{const h=()=>s(window.innerWidth<768);window.addEventListener("resize",h);return()=>window.removeEventListener("resize",h)},[]);return m}

function Ic({n,sz=18}){const d={dash:<><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></>,prosp:<><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></>,cli:<><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/></>,usr:<><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></>,emp:<><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="19" y1="8" x2="19" y2="14"/><line x1="22" y1="11" x2="16" y2="11"/></>,cal:<><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></>,out:<><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16,17 21,12 16,7"/><line x1="21" y1="12" x2="9" y2="12"/></>,plus:<><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></>,file:<><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14,2 14,8 20,8"/></>,dl:<><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7,10 12,15 17,10"/><line x1="12" y1="15" x2="12" y2="3"/></>,tr:<><polyline points="3,6 5,6 21,6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></>,back:<><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12,19 5,12 12,5"/></>,srch:<><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></>,lock:<><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></>,check:<><polyline points="20,6 9,17 4,12"/></>,todo:<><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></>,edit:<><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></>,sync:<><path d="M21 2v6h-6"/><path d="M3 12a9 9 0 0 1 15-6.7L21 8"/><path d="M3 22v-6h6"/><path d="M21 12a9 9 0 0 1-15 6.7L3 16"/></>,pipe:<><line x1="6" y1="3" x2="6" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/></>,bell:<><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></>,menu:<><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="18" x2="21" y2="18"/></>,dollar:<><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></>,clock:<><circle cx="12" cy="12" r="10"/><polyline points="12,6 12,12 16,14"/></>,copy:<><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></>};return<svg width={sz} height={sz} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">{d[n]}</svg>}

function Btn({children,onClick,v="primary",sz="md",icon,disabled,style:sx}){const b={display:"inline-flex",alignItems:"center",gap:6,border:"none",cursor:disabled?"not-allowed":"pointer",fontFamily:F,fontWeight:600,borderRadius:10,transition:"all .25s cubic-bezier(.4,0,.2,1)",opacity:disabled?.4:1,whiteSpace:"nowrap",fontSize:sz==="sm"?11:sz==="lg"?14:12,padding:sz==="sm"?"5px 10px":sz==="lg"?"12px 24px":"7px 14px"};const vs={primary:{background:`linear-gradient(135deg,${C.acc},#D4A00E)`,color:"#060B18",boxShadow:"0 3px 10px rgba(248,186,16,.2)"},secondary:{background:`linear-gradient(145deg,${C.s2},${C.s})`,color:C.tx,border:`1px solid ${C.b}`},danger:{background:`linear-gradient(145deg,${C.rBg},#120510)`,color:C.r,border:"1px solid #3D1525"},ghost:{background:"transparent",color:C.tm}};return<button onClick={onClick} disabled={disabled} style={{...b,...vs[v],...sx}} onMouseEnter={e=>{if(!disabled)e.currentTarget.style.transform="translateY(-1px)"}} onMouseLeave={e=>{e.currentTarget.style.transform=""}}>{icon&&<Ic n={icon} sz={sz==="sm"?13:15}/>}{children}</button>}
function Inp({label,...p}){return<div style={{display:"flex",flexDirection:"column",gap:5}}>{label&&<label style={{fontSize:11,fontWeight:600,color:C.tm,fontFamily:F,textTransform:"uppercase",letterSpacing:".06em"}}>{label}</label>}<input {...p} style={{background:C.bg,border:`1px solid ${C.b}`,borderRadius:8,padding:"9px 12px",color:C.tx,fontSize:13,fontFamily:F,outline:"none",width:"100%",...p.style}} onFocus={e=>e.target.style.borderColor=C.acc} onBlur={e=>e.target.style.borderColor=C.b}/></div>}
function Sel({label,options,...p}){return<div style={{display:"flex",flexDirection:"column",gap:5}}>{label&&<label style={{fontSize:11,fontWeight:600,color:C.tm,fontFamily:F,textTransform:"uppercase",letterSpacing:".06em"}}>{label}</label>}<select {...p} style={{background:C.bg,border:`1px solid ${C.b}`,borderRadius:8,padding:"9px 12px",color:C.tx,fontSize:13,fontFamily:F,outline:"none",cursor:"pointer",...p.style}}>{options.map(o=><option key={o.value} value={o.value}>{o.label||o.value}</option>)}</select></div>}
function Txt({label,...p}){return<div style={{display:"flex",flexDirection:"column",gap:5}}>{label&&<label style={{fontSize:11,fontWeight:600,color:C.tm,fontFamily:F,textTransform:"uppercase",letterSpacing:".06em"}}>{label}</label>}<textarea {...p} style={{background:C.bg,border:`1px solid ${C.b}`,borderRadius:8,padding:"9px 12px",color:C.tx,fontSize:13,fontFamily:F,outline:"none",resize:"vertical",minHeight:70,width:"100%",...p.style}} onFocus={e=>e.target.style.borderColor=C.acc} onBlur={e=>e.target.style.borderColor=C.b}/></div>}
function Mod({title,onClose,children,w=520}){return<div style={{position:"fixed",inset:0,background:"rgba(0,0,0,.7)",display:"flex",alignItems:"center",justifyContent:"center",zIndex:1000,backdropFilter:"blur(12px)"}} onClick={e=>{if(e.target===e.currentTarget)onClose()}}><div style={{background:`linear-gradient(145deg,${C.s},${C.bg})`,borderRadius:20,border:`1px solid ${C.b}`,width:"92%",maxWidth:w,maxHeight:"88vh",overflow:"auto",animation:"modalIn .3s cubic-bezier(.4,0,.2,1)",boxShadow:"0 20px 60px rgba(0,0,0,.5),0 0 30px rgba(248,186,16,.05)"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"16px 20px",borderBottom:`1px solid ${C.b}`,position:"sticky",top:0,background:`linear-gradient(145deg,${C.s},${C.bg})`,zIndex:1,borderRadius:"20px 20px 0 0"}}><h3 style={{fontFamily:D,fontSize:16,fontWeight:600,color:C.tx}}>{title}</h3><button onClick={onClose} style={{background:`linear-gradient(145deg,${C.s2},${C.s})`,border:`1px solid ${C.b}`,borderRadius:8,width:28,height:28,color:C.tm,cursor:"pointer",fontSize:14,display:"flex",alignItems:"center",justifyContent:"center",transition:"all .2s"}} onMouseEnter={e=>e.currentTarget.style.borderColor=C.acc} onMouseLeave={e=>e.currentTarget.style.borderColor=C.b}>✕</button></div><div style={{padding:20}}>{children}</div></div></div>}
function Badge({label,color}){return<span style={{display:"inline-flex",alignItems:"center",gap:4,padding:"3px 9px",borderRadius:20,fontSize:10,fontWeight:700,background:color+"18",color,fontFamily:F}}>{label}</span>}
function SBadge({status}){const s=STATUS_OPTS.find(o=>o.value===status)||STATUS_OPTS[0];return<Badge label={`${s.icon} ${s.label}`} color={s.color}/>}
function CatBadge({cat}){const c=CATS.find(x=>x.value===cat)||CATS[0];return<Badge label={`${c.icon} ${c.label}`} color={c.color}/>}
function Empty({icon,title,sub,action,actionLabel}){return<div style={{display:"flex",flexDirection:"column",alignItems:"center",padding:"48px 20px",textAlign:"center"}}><div style={{fontSize:40,marginBottom:12,opacity:.4}}>{icon}</div><div style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tm,marginBottom:4}}>{title}</div>{sub&&<div style={{fontSize:12,color:C.td,maxWidth:280,marginBottom:action?14:0}}>{sub}</div>}{action&&<button onClick={action} style={{background:`linear-gradient(135deg,${C.acc},#D4A00E)`,color:"#060B18",border:"none",borderRadius:10,padding:"8px 18px",fontSize:12,fontWeight:700,fontFamily:F,cursor:"pointer",boxShadow:"0 3px 10px rgba(248,186,16,.2)"}}>{actionLabel||"Crear"}</button>}</div>}
function Stat({label,value,icon,color,sub}){return<div style={{background:`linear-gradient(145deg,${C.s},${C.s2})`,borderRadius:16,border:`1px solid ${C.b}`,padding:"18px 20px",flex:"1 1 140px",minWidth:130,transition:"all .3s",position:"relative",overflow:"hidden"}}><div style={{position:"absolute",top:0,left:0,right:0,height:1,background:`linear-gradient(90deg,transparent,${color||C.acc}20,transparent)`}}/><div style={{display:"flex",justifyContent:"space-between",marginBottom:10}}><span style={{fontSize:10,fontWeight:700,color:C.tm,textTransform:"uppercase",letterSpacing:".08em"}}>{label}</span><span style={{fontSize:18}}>{icon}</span></div><div style={{fontFamily:D,fontSize:28,fontWeight:800,color:color||C.tx}}>{value}</div>{sub&&<div style={{fontSize:10,color:C.td,marginTop:4}}>{sub}</div>}</div>}
function Card({children,style:sx,onClick,hover}){const r=useRef(null);return<div ref={r} onClick={onClick} style={{background:`linear-gradient(145deg,${C.s},${C.s}dd)`,borderRadius:16,border:`1px solid ${C.b}`,padding:18,cursor:onClick?"pointer":"default",transition:"all .3s cubic-bezier(.4,0,.2,1)",backdropFilter:"blur(10px)",position:"relative",overflow:"hidden",...sx}} onMouseEnter={()=>{if(hover&&r.current){r.current.style.borderColor=C.acc+"40";r.current.style.transform="translateY(-3px)";r.current.style.boxShadow=`0 8px 25px rgba(0,0,0,.3),0 0 15px ${C.acc}08`}}} onMouseLeave={()=>{if(hover&&r.current){r.current.style.borderColor=C.b;r.current.style.transform="";r.current.style.boxShadow=""}}}>{children}</div>}
function FileCard({file,onDelete,showDel=true}){const info=[...DEL_TYPES,...PROSP_TYPES].find(x=>x.value===file.file_type)||{icon:"📎",label:"Archivo"};const handleDL=async()=>{if(file.url){window.open(file.url,"_blank");return}if(file.storage_path){const{data}=supabase.storage.from("wauya-files").getPublicUrl(file.storage_path);if(data?.publicUrl)window.open(data.publicUrl,"_blank")}};return<div style={{display:"flex",alignItems:"center",gap:10,padding:"10px 12px",background:C.bg,borderRadius:10,border:`1px solid ${C.b}`}}><span style={{fontSize:20,flexShrink:0}}>{info.icon}</span><div style={{flex:1,minWidth:0}}><div style={{fontSize:12,fontWeight:600,color:C.tx,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{file.name}</div><div style={{fontSize:10,color:C.td}}>{info.label} · {file.size||"—"}</div></div><div style={{display:"flex",gap:4,flexShrink:0}}><button onClick={handleDL} style={{background:C.blBg,border:`1px solid ${C.bl}30`,cursor:"pointer",color:C.bl,padding:"5px 10px",borderRadius:6,display:"flex",alignItems:"center",gap:4,fontSize:10,fontFamily:F,fontWeight:600}}><Ic n="dl" sz={13}/>Descargar</button>{showDel&&onDelete&&<button onClick={()=>onDelete(file.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.r,padding:4}}><Ic n="tr" sz={14}/></button>}</div></div>}
function MiniBar({data,height=130}){const max=Math.max(...data.map(d=>d.value),1);return<div style={{display:"flex",alignItems:"flex-end",gap:8,height,padding:"0 4px"}}>{data.map((d,i)=><div key={i} style={{flex:1,display:"flex",flexDirection:"column",alignItems:"center",gap:4}}><span style={{fontSize:11,fontWeight:700,color:C.tx}}>{d.value}</span><div style={{width:"100%",maxWidth:44,height:`${(d.value/max)*(height-40)}px`,minHeight:4,background:`linear-gradient(180deg,${d.color},${d.color}88)`,borderRadius:"5px 5px 2px 2px",transition:"height .4s"}}/><span style={{fontSize:9,color:C.td,textAlign:"center",lineHeight:1.2}}>{d.label}</span></div>)}</div>}
function Donut({data,size=110}){const total=data.reduce((a,d)=>a+d.value,0)||1;let cum=0;const r=size/2-10,cx=size/2,cy=size/2;return<svg width={size} height={size} viewBox={`0 0 ${size} ${size}`}>{data.filter(d=>d.value>0).map((d,i)=>{const s0=cum/total*360;cum+=d.value;const e0=cum/total*360;const g=2,s=(s0+g/2)*Math.PI/180,e=(e0-g/2)*Math.PI/180;if(e0-s0<1)return null;const la=e0-s0>180?1:0;const path=`M ${cx+r*Math.cos(s)} ${cy+r*Math.sin(s)} A ${r} ${r} 0 ${la} 1 ${cx+r*Math.cos(e)} ${cy+r*Math.sin(e)}`;return<path key={i} d={path} fill="none" stroke={d.color} strokeWidth={12} strokeLinecap="round"/>})}<text x={cx} y={cy-3} textAnchor="middle" fill={C.tx} fontFamily={D} fontWeight="800" fontSize="18">{total}</text><text x={cx} y={cy+11} textAnchor="middle" fill={C.td} fontFamily={F} fontSize="8">TOTAL</text></svg>}
function TaskRow({td,employees,onStatusChange,onEdit,onDelete,showProject,clients,prospects,onLogTime,onDuplicate}){const emp=employees.find(e=>e.id===td.assigned_to);const emp2=employees.find(e=>e.id===td.assigned_to_2);const pr=PRIOS.find(p=>p.value===td.priority);const st=TSTAT.find(s=>s.value===td.status);const cat=CATS.find(c=>c.value===(td.category||"cliente"))||CATS[0];const projName=td.client_id?clients?.find(x=>x.id===td.client_id)?.company:td.prospect_id?prospects?.find(x=>x.id===td.prospect_id)?.company:"Interno";return<div style={{display:"flex",alignItems:"center",gap:8,padding:"9px 12px",background:C.bg,borderRadius:10,border:`1px solid ${C.b}`,flexWrap:"wrap"}}><select value={td.status} onChange={e=>onStatusChange(td.id,{status:e.target.value})} style={{background:st?.color+"20",border:`1px solid ${st?.color}40`,borderRadius:6,padding:"3px 6px",color:st?.color,fontSize:10,fontWeight:700,fontFamily:F,cursor:"pointer",outline:"none"}}>{TSTAT.map(s=><option key={s.value} value={s.value}>{s.label}</option>)}</select><div style={{flex:1,minWidth:0}}><div style={{display:"flex",alignItems:"center",gap:6,flexWrap:"wrap"}}><span style={{fontSize:12,fontWeight:600,color:td.status==="completado"?C.td:C.tx,textDecoration:td.status==="completado"?"line-through":"none"}}>{td.title}</span>{td.project_type&&<span style={{fontSize:9,color:C.td,background:C.bg,padding:"1px 5px",borderRadius:4,border:`1px solid ${C.b}`}}>{td.project_type}</span>}</div><div style={{fontSize:10,color:C.td,display:"flex",gap:8,marginTop:1,flexWrap:"wrap"}}>{showProject&&projName&&<span style={{color:cat.color}}>{cat.icon} {projName}</span>}{emp&&<span>👤 {emp.name}</span>}{emp2&&<span style={{color:C.p}}>+ {emp2.name}</span>}{td.end_date&&<span>📅 {td.end_date}</span>}{td.url&&<a href={td.url} target="_blank" rel="noopener noreferrer" style={{color:C.bl,textDecoration:"none"}}>🔗</a>}</div></div><div style={{display:"flex",gap:3,alignItems:"center",flexShrink:0}}>{onLogTime&&<Btn onClick={()=>onLogTime(td)} v="ghost" sz="sm" icon="clock"/>}{onDuplicate&&<Btn onClick={()=>onDuplicate(td)} v="ghost" sz="sm" icon="copy"/>}<CatBadge cat={td.category||"cliente"}/>{pr&&<Badge label={pr.label} color={pr.color}/>}{onEdit&&<Btn onClick={()=>onEdit(td)} v="ghost" sz="sm" icon="edit"/>}{onDelete&&<button onClick={()=>onDelete(td.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.r,padding:3}}><Ic n="tr" sz={13}/></button>}</div></div>}
function Timeline({tasks,employees}){const dated=tasks.filter(t=>t.start_date);if(!dated.length)return<Empty icon="📅" title="Sin fechas"/>;const sorted=[...dated].sort((a,b)=>a.start_date.localeCompare(b.start_date));const allD=sorted.flatMap(t=>[t.start_date,t.end_date].filter(Boolean));const minD=new Date(Math.min(...allD.map(d=>new Date(d)))),maxD=new Date(Math.max(...allD.map(d=>new Date(d))));const diff=Math.max(Math.ceil((maxD-minD)/864e5),1),dW=Math.max(30,700/diff);const getP=d=>((new Date(d)-minD)/864e5)*dW;const days=[];for(let d=new Date(minD);d<=maxD;d.setDate(d.getDate()+1))days.push(new Date(d));return<div style={{overflowX:"auto",borderRadius:10,border:`1px solid ${C.b}`}}><div style={{minWidth:days.length*dW+200}}><div style={{display:"flex",borderBottom:`1px solid ${C.b}`,position:"sticky",top:0,background:C.s,zIndex:2}}><div style={{width:200,flexShrink:0,padding:"7px 12px",fontSize:10,fontWeight:700,color:C.tm,textTransform:"uppercase",borderRight:`1px solid ${C.b}`}}>Tarea</div><div style={{display:"flex",flex:1}}>{days.map((d,i)=>{const isT=d.toDateString()===new Date().toDateString();return<div key={i} style={{width:dW,flexShrink:0,padding:"5px 2px",textAlign:"center",fontSize:9,fontWeight:isT?700:400,color:isT?C.acc:C.td,borderRight:`1px solid ${C.b}10`,background:isT?C.acc+"08":"transparent"}}><div>{d.toLocaleDateString("es",{weekday:"short"}).slice(0,2)}</div><div style={{fontSize:10,fontWeight:700}}>{d.getDate()}</div></div>})}</div></div>{sorted.map((task,i)=>{const emp=employees.find(e=>e.id===task.assigned_to);const emp2=employees.find(e=>e.id===task.assigned_to_2);const st=TSTAT.find(s=>s.value===task.status);const cat=CATS.find(c=>c.value===(task.category||"cliente"))||CATS[0];const start=getP(task.start_date);const width=task.end_date?Math.max(getP(task.end_date)-start+dW,dW):dW;return<div key={task.id||i} style={{display:"flex",borderBottom:`1px solid ${C.b}10`,minHeight:40,alignItems:"center"}}><div style={{width:200,flexShrink:0,padding:"5px 12px",borderRight:`1px solid ${C.b}`}}><div style={{fontSize:11,fontWeight:600,color:C.tx,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{task.title}</div><div style={{fontSize:9,color:C.td}}>{emp?.name||"Sin asignar"}{emp2?" + "+emp2.name:""} · <span style={{color:cat.color}}>{cat.icon}</span></div></div><div style={{flex:1,position:"relative",height:40,display:"flex",alignItems:"center"}}><div style={{position:"absolute",left:start,width,height:20,borderRadius:5,background:`linear-gradient(90deg,${cat.color}50,${cat.color}25)`,border:`1px solid ${cat.color}35`,display:"flex",alignItems:"center",padding:"0 6px"}}><span style={{fontSize:9,fontWeight:600,color:cat.color,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{task.title}</span></div></div></div>})}</div></div>}
function SearchResults({q,prospects,clients,employees,todos,onSelect,onClose}){if(!q||q.length<2)return null;const lq=q.toLowerCase();const rp=prospects.filter(x=>(x.name+x.company+(x.email||"")).toLowerCase().includes(lq)).slice(0,3);const rc=clients.filter(x=>(x.name+x.company+(x.email||"")).toLowerCase().includes(lq)).slice(0,3);const re=employees.filter(x=>(x.name+(x.email||"")).toLowerCase().includes(lq)).slice(0,3);const rt=todos.filter(x=>(x.title+(x.description||"")).toLowerCase().includes(lq)).slice(0,4);const total=rp.length+rc.length+re.length+rt.length;if(!total)return<div style={{position:"absolute",top:"100%",left:0,right:0,background:C.s,border:`1px solid ${C.b}`,borderRadius:10,padding:16,zIndex:100,marginTop:4,textAlign:"center"}}><span style={{fontSize:12,color:C.td}}>Sin resultados</span></div>;return<div style={{position:"absolute",top:"100%",left:0,right:0,background:C.s,border:`1px solid ${C.b}`,borderRadius:10,zIndex:100,marginTop:4,maxHeight:400,overflow:"auto"}}>{rp.length>0&&<div style={{padding:"8px 14px",borderBottom:`1px solid ${C.b}`}}><div style={{fontSize:10,fontWeight:700,color:C.tm,marginBottom:6}}>🎯 PROSPECTOS</div>{rp.map(p=><div key={p.id} onClick={()=>{onSelect("prospects",p.id);onClose()}} style={{padding:"6px 8px",borderRadius:6,cursor:"pointer",fontSize:12,color:C.tx}} onMouseEnter={e=>e.currentTarget.style.background=C.bg} onMouseLeave={e=>e.currentTarget.style.background="transparent"}>{p.company||p.name}</div>)}</div>}{rc.length>0&&<div style={{padding:"8px 14px",borderBottom:`1px solid ${C.b}`}}><div style={{fontSize:10,fontWeight:700,color:C.tm,marginBottom:6}}>💼 CLIENTES</div>{rc.map(c=><div key={c.id} onClick={()=>{onSelect("clients",c.id);onClose()}} style={{padding:"6px 8px",borderRadius:6,cursor:"pointer",fontSize:12,color:C.tx}} onMouseEnter={e=>e.currentTarget.style.background=C.bg} onMouseLeave={e=>e.currentTarget.style.background="transparent"}>{c.company||c.name}</div>)}</div>}{rt.length>0&&<div style={{padding:"8px 14px"}}><div style={{fontSize:10,fontWeight:700,color:C.tm,marginBottom:6}}>📋 TAREAS</div>{rt.map(t=><div key={t.id} onClick={()=>{onSelect(t.client_id?"clients":"timeline",t.client_id);onClose()}} style={{padding:"6px 8px",borderRadius:6,cursor:"pointer",fontSize:12,color:C.tx}} onMouseEnter={e=>e.currentTarget.style.background=C.bg} onMouseLeave={e=>e.currentTarget.style.background="transparent"}>{t.title}</div>)}</div>}</div>}

// ═══ MAIN APP ═══
export default function App(){return<ErrorBoundary><AppInner/></ErrorBoundary>}
function AppInner(){
  const isMobile=useIsMobile();
  const[loading,setLoading]=useState(true);const[user,setUser]=useState(null);const[view,setView]=useState("dashboard");
  const[prospects,setProspects]=useState([]);const[clients,setClients]=useState([]);const[employees,setEmployees]=useState([]);
  const[clientUsers,setCU]=useState([]);const[todos,setTodos]=useState([]);const[files,setFiles]=useState([]);
  const[selId,setSelId]=useState(null);const[modal,setModal]=useState(null);const[search,setSearch]=useState("");
  const[loginForm,setLF]=useState({email:"",password:""});const[loginErr,setLE]=useState("");
  const[saving,setSaving]=useState(false);const[syncStatus,setSyncStatus]=useState("");
  const[toast,setToast]=useState(null);
  const showToast=(msg,type="success")=>{setToast({msg,type});setTimeout(()=>setToast(null),2500)};
  const autoSave=useDebounce(async(table,id,upd)=>{await supabase.from(table).update(upd).eq("id",id);showToast("Guardado automáticamente");await loadAll()},900);
  const[mobileMenu,setMM]=useState(false);const[globalSearch,setGS]=useState("");const[gsOpen,setGSOpen]=useState(false);
  const[notifOpen,setNO]=useState(false);const[approvals,setApprovals]=useState([]);
  // LOT 2 STATE
  const[timeEntries,setTE]=useState([]);const[templates,setTemplates]=useState([]);const[activityLog,setAL]=useState([]);
  // Client portal state (must be top-level for hooks rules)
  const[obStep,setObStep]=useState(0);const[creds,setCreds]=useState([]);
  const[credForm,setCredForm]=useState({platform:"instagram",username:"",password:"",url:"",notes:""});
  const[showPwMap,setShowPwMap]=useState({});const[calComment,setCalComment]=useState("");const[brandComment,setBrandComment]=useState("");
  const[cmdOpen,setCmdOpen]=useState(false);const[cmdQuery,setCmdQuery]=useState("");const[fabOpen,setFabOpen]=useState(false);

  const safeQuery=async(table,opts={})=>{try{let q=supabase.from(table).select("*");if(opts.order)q=q.order(opts.order,{ascending:opts.asc??false});if(opts.limit)q=q.limit(opts.limit);const{data}=await q;return data||[]}catch{return[]}};
  const loadAll=useCallback(async()=>{
    setProspects(await safeQuery("prospects",{order:"created_at"}));
    setClients(await safeQuery("clients",{order:"created_at"}));
    setEmployees(await safeQuery("employees",{order:"created_at"}));
    setCU(await safeQuery("client_users"));
    setTodos(await safeQuery("todos",{order:"created_at"}));
    setFiles(await safeQuery("files",{order:"created_at"}));
    setApprovals(await safeQuery("approvals",{order:"created_at"}));
    setTE(await safeQuery("time_entries",{order:"created_at"}));
    setTemplates(await safeQuery("task_templates",{order:"name",asc:true}));
    setAL(await safeQuery("activity_log",{order:"created_at",limit:50}));
  },[]);
  useEffect(()=>{
    // Global error handler - prevents white screen
    const onErr=e=>{e.preventDefault();console.error("Wauya caught:",e.reason||e.message)};
    window.addEventListener("unhandledrejection",onErr);
    try{const sess=localStorage.getItem("wauya_user");if(sess){const parsed=JSON.parse(sess);if(parsed&&parsed.role)setUser(parsed);else localStorage.removeItem("wauya_user")}}catch{localStorage.removeItem("wauya_user")}loadAll().catch(()=>{}).finally(()=>setLoading(false));
    return()=>window.removeEventListener("unhandledrejection",onErr);
  },[loadAll]);

  // Cmd+K listener
  useEffect(()=>{const h=e=>{if((e.metaKey||e.ctrlKey)&&e.key==="k"){e.preventDefault();setCmdOpen(o=>!o);setCmdQuery("")}if(e.key==="Escape")setCmdOpen(false)};window.addEventListener("keydown",h);return()=>window.removeEventListener("keydown",h)},[]);

  // Client Health Score (0-100)
  const getHealthScore=(cl)=>{try{let score=50;const clTodos=todos.filter(t=>t.client_id===cl.id);const total=clTodos.length||1;const done=clTodos.filter(t=>t.status==="completado").length;score+=Math.round((done/total)*20);if(cl.brand_status==="aprobado")score+=10;if(cl.calendar_status==="aprobado")score+=10;if(cl.billing_status==="activo")score+=10;else if(cl.billing_status==="vencido")score-=20;const overdue=clTodos.filter(t=>t.end_date&&t.status!=="completado"&&t.end_date<todayStr()).length;score-=overdue*5;return Math.max(0,Math.min(100,score))}catch{return 50}};
  const getHealthColor=(score)=>score>=70?C.g:score>=40?C.w:C.r;
  const getHealthLabel=(score)=>score>=70?"Saludable":score>=40?"Atención":"Riesgo";
  useEffect(()=>{if(user?.role==="client"&&user?.client_id){supabase.from("client_credentials").select("*").eq("client_id",user.client_id).order("created_at").then(({data})=>{if(data)setCreds(data)}).catch(()=>{})}},[user?.role,user?.client_id]);

  // Client portal: trigger onboarding if not done
  useEffect(()=>{if(user?.role==="client"){const cu=clientUsers.find(u=>u.id===user.id);if(cu&&!cu.onboarding_done&&obStep===0)setObStep(1)}},[user,clientUsers]);

  // Activity logger
  const logAct=async(action,entityType,entityName)=>{try{await supabase.from("activity_log").insert({action,entity_type:entityType,entity_name:entityName,user_name:user?.name||"Admin"})}catch{}};

  const notifs=useMemo(()=>{const n=[];const pendApp=approvals.filter(a=>a.status==="cambios");pendApp.forEach(a=>{const cl=clients.find(x=>x.id===a.client_id);n.push({id:"ap-"+a.id,icon:"✏️",msg:`${cl?.company||"Cliente"} pidió cambios en "${a.title}"`,color:C.r,view:"clients",linkId:a.client_id,time:a.created_at})});const overdue=todos.filter(t=>t.end_date&&t.status!=="completado"&&t.end_date<todayStr());overdue.forEach(t=>n.push({id:"od-"+t.id,icon:"🔴",msg:`Tarea vencida: "${t.title}"`,color:C.r,view:t.client_id?"clients":"timeline",linkId:t.client_id,time:t.end_date}));const unassigned=todos.filter(t=>!t.assigned_to&&t.status!=="completado");if(unassigned.length>0)n.push({id:"ua",icon:"👤",msg:`${unassigned.length} tareas sin asignar`,color:C.p,view:"timeline",time:new Date().toISOString()});const billing=clients.filter(c=>c.billing_status==="vencido");if(billing.length>0)n.push({id:"bill",icon:"💰",msg:`${billing.length} pago(s) vencido(s)`,color:C.r,view:"clients",time:new Date().toISOString()});return n.sort((a,b)=>new Date(b.time)-new Date(a.time))},[approvals,todos,clients]);

  const login=async()=>{setLE("");try{const{data:admin}=await supabase.from("admin_users").select("*").eq("email",loginForm.email).eq("password",loginForm.password).single();if(admin){const u={...admin,role:"master"};delete u.password;setUser(u);localStorage.setItem("wauya_user",JSON.stringify(u));setView("dashboard");return}}catch{}try{const{data:cu}=await supabase.from("client_users").select("*").eq("email",loginForm.email).eq("password",loginForm.password).single();if(cu){const u={...cu,role:"client"};setUser(u);localStorage.setItem("wauya_user",JSON.stringify(u));setView("client_portal");return}}catch{}try{const{data:emp}=await supabase.from("employees").select("*").eq("email",loginForm.email).eq("password",loginForm.password).single();if(emp){const u={...emp,role:"employee"};setUser(u);localStorage.setItem("wauya_user",JSON.stringify(u));setView("emp_portal");return}}catch{}setLE("Credenciales incorrectas")};
  const logout=()=>{setUser(null);setView("dashboard");setSelId(null);localStorage.removeItem("wauya_user")};
  const dbAdd=async(table,row)=>{try{setSaving(true);const{data,error}=await supabase.from(table).insert(row).select().single();setSaving(false);if(error){showToast("Error: "+error.message,"error");return null}await loadAll();return data}catch(e){setSaving(false);showToast("Error: "+e.message,"error");return null}};
  const dbUpd=async(table,id,upd)=>{try{setSaving(true);await supabase.from(table).update(upd).eq("id",id);setSaving(false);await loadAll()}catch(e){setSaving(false);showToast("Error: "+e.message,"error")}};
  const dbDel=async(table,id)=>{try{setSaving(true);await supabase.from(table).delete().eq("id",id);setSaving(false);await loadAll()}catch(e){setSaving(false);showToast("Error: "+e.message,"error")}};
  const uploadFile=async(file,ot,oid,ft)=>{try{setSaving(true);const ext=file.name.split(".").pop();const path=`${ot}/${oid}/${Date.now()}.${ext}`;const{error:e}=await supabase.storage.from("wauya-files").upload(path,file);if(e){setSaving(false);showToast(e.message,"error");return}const{data:u}=supabase.storage.from("wauya-files").getPublicUrl(path);await supabase.from("files").insert({owner_type:ot,owner_id:oid,name:file.name,file_type:ft,size:(file.size/1024).toFixed(1)+" KB",storage_path:path,url:u?.publicUrl||""});setSaving(false);await loadAll();setModal(null);showToast("Archivo subido")}catch(e){setSaving(false);showToast("Error: "+e.message,"error")}};
  const deleteFile=async fid=>{try{const f=files.find(x=>x.id===fid);if(f?.storage_path)await supabase.storage.from("wauya-files").remove([f.storage_path]);await dbDel("files",fid)}catch(e){showToast("Error: "+e.message,"error")}};
  const addProspect=async p=>{const d=await dbAdd("prospects",{...p,pipeline_status:"nuevo"});if(d){await logAct("Creó prospecto","prospect",p.company||p.name);showToast("Prospecto creado")}setModal(null)};
  const updProspect=async(id,u)=>dbUpd("prospects",id,u);
  const delProspect=async id=>{const p=prospects.find(x=>x.id===id);if(!confirm("¿Eliminar?"))return;await dbDel("prospects",id);if(p)await logAct("Eliminó prospecto","prospect",p.company||p.name);if(selId===id)setSelId(null)};
  const convertProspect=async(id,onboardingData)=>{const p=prospects.find(x=>x.id===id);if(!p)return;const clientData={name:p.name,company:p.company,email:p.email,phone:p.phone,services:p.services,notes:p.notes,status:"briefing",drive_folder_id:p.drive_folder_id,...(onboardingData||{})};await dbAdd("clients",clientData);await dbDel("prospects",id);await logAct("Convirtió a cliente","client",p.company||p.name);setSelId(null);setView("clients");setModal(null)};
  const moveProspect=async(id,status)=>dbUpd("prospects",id,{pipeline_status:status});
  const addClient=async cl=>{const d=await dbAdd("clients",cl);if(d){await logAct("Creó cliente","client",cl.company||cl.name);showToast("Cliente creado")}setModal(null)};
  const updClient=async(id,u)=>dbUpd("clients",id,u);
  const delClient=async id=>{const c=clients.find(x=>x.id===id);if(!confirm("¿Eliminar?"))return;await dbDel("clients",id);if(c)await logAct("Eliminó cliente","client",c.company||c.name);if(selId===id)setSelId(null)};
  const addCU=async(cid,u)=>{await dbAdd("client_users",{client_id:cid,...u});await logAct("Creó usuario","client_user",u.name);setModal(null)};
  const delCU=async uid=>{if(!confirm("¿Eliminar?"))return;await dbDel("client_users",uid)};
  const addEmp=async e=>{const d=await dbAdd("employees",e);if(d){await logAct("Agregó empleado","employee",e.name);showToast("Empleado agregado")}setModal(null)};
  const delEmp=async id=>{const e=employees.find(x=>x.id===id);if(!confirm("¿Eliminar?"))return;await dbDel("employees",id);if(e)await logAct("Eliminó empleado","employee",e.name)};
  const addTodo=async td=>{const d=await dbAdd("todos",td);if(d){await logAct("Creó tarea","todo",td.title);showToast("Tarea creada")}setModal(null)};
  const updTodo=async(id,u)=>{await dbUpd("todos",id,u);if(u.status==="completado"){const t=todos.find(x=>x.id===id);if(t)await logAct("Completó tarea","todo",t.title)}};
  const delTodo=async id=>{const t=todos.find(x=>x.id===id);if(!confirm("¿Eliminar?"))return;await dbDel("todos",id);if(t)await logAct("Eliminó tarea","todo",t.title)};

  // DUPLICATE single task (shifts dates by 7 days by default)
  const dupTodo=async(td,dayShift=7)=>{const newStart=td.start_date?shiftDate(td.start_date,dayShift):"";const newEnd=td.end_date?shiftDate(td.end_date,dayShift):"";const row={title:td.title,description:td.description,priority:td.priority,status:"pendiente",assigned_to:td.assigned_to||null,assigned_to_2:td.assigned_to_2||null,start_date:newStart,end_date:newEnd,category:td.category,client_id:td.client_id||null,prospect_id:td.prospect_id||null,url:td.url,project_type:td.project_type};await dbAdd("todos",row);await logAct("Duplicó tarea","todo",td.title);showToast("Tarea duplicada (+7 días)")};
  function shiftDate(dateStr,days){const d=new Date(dateStr);d.setDate(d.getDate()+days);return d.toISOString().split("T")[0]}

  // DUPLICATE multiple tasks (week or period)
  const dupPeriod=async(tasksToClone,dayShift)=>{try{if(!tasksToClone.length)return;setSaving(true);for(const td of tasksToClone){const newStart=td.start_date?shiftDate(td.start_date,dayShift):"";const newEnd=td.end_date?shiftDate(td.end_date,dayShift):"";await supabase.from("todos").insert({title:td.title,description:td.description,priority:td.priority,status:"pendiente",assigned_to:td.assigned_to||null,assigned_to_2:td.assigned_to_2||null,start_date:newStart,end_date:newEnd,category:td.category,client_id:td.client_id||null,prospect_id:td.prospect_id||null,url:td.url,project_type:td.project_type})}await logAct("Duplicó "+tasksToClone.length+" tareas","todo","periodo +"+dayShift+" días");setSaving(false);await loadAll();showToast(`${tasksToClone.length} tareas duplicadas (+${dayShift} días)`)}catch(e){setSaving(false);showToast("Error: "+e.message,"error")}};

  // TIME TRACKING
  const addTimeEntry=async(entry)=>{await dbAdd("time_entries",entry);await logAct("Registró "+entry.hours+"h","time",entry.description||"");showToast(entry.hours+"h registradas");setModal(null)};

  // APPLY TEMPLATE
  const applyTemplate=async(templateId,targetType,targetId)=>{try{
    const tpl=templates.find(t=>t.id===templateId);if(!tpl)return;
    setSaving(true);const tasks=typeof tpl.tasks==="string"?JSON.parse(tpl.tasks):tpl.tasks;
    for(const task of tasks){
      const row={title:task.title,priority:task.priority||"media",status:"pendiente",category:tpl.category||"cliente",start_date:todayStr()};
      if(targetType==="client")row.client_id=targetId;
      else if(targetType==="prospect")row.prospect_id=targetId;
      await supabase.from("todos").insert(row);
    }
    await logAct("Aplicó template","template",tpl.name+" ("+tasks.length+" tareas)");
    setSaving(false);await loadAll();setModal(null);showToast(`${tasks.length} tareas creadas desde "${tpl.name}"`);
  }catch(e){setSaving(false);showToast("Error: "+e.message,"error")}};

  const syncFromDrive=async folderUrl=>{const fid=extractFolderId(folderUrl);if(!fid){showToast("URL no válida","error");return}setSaving(true);setSyncStatus("Escaneando...");try{const res=await fetch(`/api/drive?action=list&folderId=${fid}`);const items=await res.json();if(items.error){showToast(items.error,"error");setSaving(false);setSyncStatus("");return}const folders=items.filter(f=>f.mimeType==="application/vnd.google-apps.folder");let cr=0,sk=0;for(const fo of folders){if(prospects.find(p=>(p.company||"").toLowerCase()===fo.name.toLowerCase())||clients.find(c=>(c.company||"").toLowerCase()===fo.name.toLowerCase())){sk++;continue}await supabase.from("prospects").insert({name:fo.name,company:fo.name,drive_folder_id:fo.id,pipeline_status:"nuevo"});cr++}await loadAll();setSyncStatus("");setSaving(false);setModal(null);localStorage.setItem("wauya_leads_folder",folderUrl);showToast(`${cr} creados · ${sk} existían`)}catch(e){showToast(e.message,"error");setSaving(false);setSyncStatus("")}};
  const handleQBR=async client=>{try{if(!client?.sheet_id){generateQBR(client,null);return}const r=await fetch(`/api/sheets?sheetId=${client.sheet_id}&range=KPIs!A1:Z50`);const d=await r.json();generateQBR(client,Array.isArray(d)?d:null)}catch{generateQBR(client,null)}};

  const prospFiles=pid=>files.filter(f=>f.owner_type==="prospect"&&f.owner_id===pid);
  const clientFiles=cid=>files.filter(f=>f.owner_type==="client"&&f.owner_id===cid);
  const totalRevenue=clients.reduce((s,c)=>s+(parseFloat(c.monthly_fee)||0),0);
  const totalHours=timeEntries.reduce((s,t)=>s+(parseFloat(t.hours)||0),0);

  // PROFITABILITY per client
  const clientProfit=useMemo(()=>clients.map(cl=>{
    const hours=timeEntries.filter(t=>t.client_id===cl.id).reduce((s,t)=>s+(parseFloat(t.hours)||0),0);
    const cost=timeEntries.filter(t=>t.client_id===cl.id).reduce((s,t)=>{const emp=employees.find(e=>e.id===t.employee_id);return s+((parseFloat(t.hours)||0)*(parseFloat(emp?.hourly_rate)||0))},0);
    const revenue=parseFloat(cl.monthly_fee)||0;
    const margin=revenue>0?Math.round(((revenue-cost)/revenue)*100):0;
    return{...cl,hours,cost,revenue,margin};
  }),[clients,timeEntries,employees]);

  const stats=useMemo(()=>{const tt=todos.length,ct=todos.filter(x=>x.status==="completado").length,pt=todos.filter(x=>x.status==="pendiente").length,ip=todos.filter(x=>x.status==="en_progreso").length;const sc=STATUS_OPTS.map(s=>({...s,count:clients.filter(x=>x.status===s.value).length}));const el=employees.map(e=>({...e,tc:todos.filter(td=>td.assigned_to===e.id&&td.status!=="completado").length,hrs:timeEntries.filter(t=>t.employee_id===e.id).reduce((s,t)=>s+(parseFloat(t.hours)||0),0)}));return{tt,ct,pt,ip,sc,el}},[todos,clients,employees,timeEntries]);

  // LOGIN
  if(!user&&!loading)return<div style={{fontFamily:F,background:`radial-gradient(ellipse at 50% 0%,${C.s2},${C.bg})`,minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",padding:20}}><style>{CSS}</style><div style={{animation:"fadeUp .5s ease",textAlign:"center",width:"100%",maxWidth:380}}><div style={{marginBottom:36}}><div style={{fontFamily:D,fontSize:48,fontWeight:800,color:C.tx}}>W<span style={{color:C.acc}}>.</span></div><div style={{fontFamily:D,fontSize:12,fontWeight:500,color:C.tm,letterSpacing:".15em",textTransform:"uppercase"}}>Wauya Platform</div></div><div style={{background:`linear-gradient(145deg,${C.s},${C.bg})`,borderRadius:20,border:`1px solid ${C.b}`,padding:isMobile?20:28,boxShadow:"0 20px 60px rgba(0,0,0,.4)",textAlign:"left"}}><h2 style={{fontFamily:D,fontSize:18,fontWeight:700,color:C.tx,marginBottom:22}}>Iniciar Sesión</h2><div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Email" type="email" value={loginForm.email} onChange={e=>setLF({...loginForm,email:e.target.value})} placeholder="tu@email.com" onKeyDown={e=>e.key==="Enter"&&login()}/><Inp label="Contraseña" type="password" value={loginForm.password} onChange={e=>setLF({...loginForm,password:e.target.value})} placeholder="••••••••" onKeyDown={e=>e.key==="Enter"&&login()}/>{loginErr&&<div style={{color:C.r,fontSize:12}}>{loginErr}</div>}<Btn onClick={login} sz="lg" style={{width:"100%",justifyContent:"center",marginTop:6}}>Ingresar</Btn></div></div><div style={{marginTop:24,fontSize:8,color:C.td,lineHeight:1.5,textAlign:"center"}}>© {new Date().getFullYear()} Wuaya Marketing & Communications. Todos los derechos reservados. Plataforma propietaria protegida por derechos de autor y propiedad intelectual.</div></div></div>;
  if(loading)return<div style={{fontFamily:F,background:C.bg,minHeight:"100vh",display:"flex"}}><style>{CSS}</style><div style={{width:220,borderRight:`1px solid ${C.b}`,background:`linear-gradient(180deg,${C.s},${C.bg})`,padding:20}}><Skeleton w={60} h={28} r={8}/><div style={{marginTop:24,display:"flex",flexDirection:"column",gap:8}}>{[1,2,3,4,5,6].map(i=><Skeleton key={i} w="100%" h={14} r={6}/>)}</div></div><div style={{flex:1,padding:"24px 32px"}}><Skeleton w={200} h={24} r={8}/><div style={{marginTop:8}}><Skeleton w={140} h={12}/></div><div style={{display:"flex",gap:12,marginTop:24}}>{[1,2,3,4].map(i=><div key={i} style={{flex:1}}><SkeletonCard/></div>)}</div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:16,marginTop:20}}><SkeletonCard/><SkeletonCard/></div></div></div>;

  // EMPLOYEE PORTAL with time logging
  if(user?.role==="employee"){const myTodos=todos.filter(td=>td.assigned_to===user.id||td.assigned_to_2===user.id);const myHours=timeEntries.filter(t=>t.employee_id===user.id);const todayHours=myHours.filter(t=>t.date===todayStr()).reduce((s,t)=>s+(parseFloat(t.hours)||0),0);const byCategory={cliente:[],propuesta:[],interno:[]};myTodos.forEach(td=>{const cat=td.category||"cliente";if(!byCategory[cat])byCategory[cat]=[];byCategory[cat].push(td)});return<div style={{fontFamily:F,background:C.bg,minHeight:"100vh"}}><style>{CSS}</style><div style={{borderBottom:`1px solid ${C.b}`,padding:isMobile?"10px 16px":"14px 28px",display:"flex",justifyContent:"space-between",alignItems:"center"}}><div style={{display:"flex",alignItems:"center",gap:14}}><span style={{fontFamily:D,fontSize:22,fontWeight:800,color:C.tx}}>W<span style={{color:C.acc}}>.</span></span><span style={{color:C.td}}>|</span><span style={{fontSize:13,color:C.tm}}>Portal Empleado</span></div><div style={{display:"flex",alignItems:"center",gap:14}}><span style={{fontSize:12,color:C.tm}}>{user.name}</span><Btn onClick={logout} v="ghost" icon="out" sz="sm">Salir</Btn></div></div><div style={{maxWidth:900,margin:"0 auto",padding:isMobile?"20px 16px":"32px 20px",animation:"fadeUp .4s ease"}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx,marginBottom:6}}>Hola, {user.name} 👋</h1><p style={{fontSize:13,color:C.tm,marginBottom:20}}>{myTodos.filter(td=>td.status!=="completado").length} pendientes · ⏱ {todayHours.toFixed(1)}h hoy</p>{/* Quick time entry */}<Card style={{marginBottom:20,background:C.s2}}><div style={{display:"flex",alignItems:"center",gap:10,flexWrap:"wrap"}}><Ic n="clock" sz={16}/><span style={{fontSize:12,fontWeight:600,color:C.tx}}>Registrar horas</span><select id="emp-task-sel" style={{flex:1,minWidth:120,background:C.bg,border:`1px solid ${C.b}`,borderRadius:6,padding:"6px 8px",color:C.tx,fontSize:11,fontFamily:F,outline:"none"}}><option value="">— Tarea —</option>{myTodos.filter(t=>t.status!=="completado").map(t=><option key={t.id} value={t.id}>{t.title}</option>)}</select><input id="emp-hours" type="number" step="0.5" min="0.5" max="12" placeholder="Hrs" style={{width:60,background:C.bg,border:`1px solid ${C.b}`,borderRadius:6,padding:"6px 8px",color:C.tx,fontSize:12,fontFamily:F,outline:"none"}}/><Btn onClick={async()=>{const tid=document.getElementById("emp-task-sel").value;const hrs=parseFloat(document.getElementById("emp-hours").value);if(!hrs||hrs<=0)return;const task=tid?myTodos.find(t=>t.id===tid):null;await addTimeEntry({employee_id:user.id,todo_id:tid||null,client_id:task?.client_id||null,hours:hrs,date:todayStr(),description:task?.title||"Trabajo general"});document.getElementById("emp-hours").value=""}} sz="sm">Registrar</Btn></div></Card>{myTodos.length===0?<Empty icon="✅" title="Sin tareas"/>:CATS.map(cat=>{const catTodos=byCategory[cat.value]||[];if(!catTodos.length)return null;return<Card key={cat.value} style={{marginBottom:16}}><h3 style={{fontFamily:D,fontSize:14,fontWeight:700,color:cat.color,marginBottom:14}}>{cat.icon} {cat.label}s ({catTodos.length})</h3><div style={{display:"flex",flexDirection:"column",gap:6}}>{catTodos.map(td=>{const pr=PRIOS.find(p=>p.value===td.priority);const st=TSTAT.find(s=>s.value===td.status);const projName=td.client_id?clients.find(x=>x.id===td.client_id)?.company:td.prospect_id?prospects.find(x=>x.id===td.prospect_id)?.company:"";const tdHrs=timeEntries.filter(t=>t.todo_id===td.id).reduce((s,t)=>s+(parseFloat(t.hours)||0),0);return<div key={td.id} style={{display:"flex",alignItems:"center",gap:10,padding:"10px 12px",background:C.bg,borderRadius:10,border:`1px solid ${C.b}`,flexWrap:"wrap"}}><button onClick={async()=>{const nx=td.status==="pendiente"?"en_progreso":td.status==="en_progreso"?"completado":"pendiente";await updTodo(td.id,{status:nx})}} style={{width:22,height:22,borderRadius:6,border:`2px solid ${st?.color}`,background:td.status==="completado"?C.g:"transparent",cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0,color:"#fff"}}>{td.status==="completado"&&<Ic n="check" sz={12}/>}</button><div style={{flex:1,minWidth:0}}><div style={{fontSize:13,fontWeight:500,color:td.status==="completado"?C.td:C.tx,textDecoration:td.status==="completado"?"line-through":"none"}}>{td.title}</div><div style={{fontSize:10,color:C.td,display:"flex",gap:8,marginTop:2,flexWrap:"wrap"}}>{projName&&<span style={{color:cat.color}}>{projName}</span>}{td.end_date&&<span>📅 {td.end_date}</span>}{tdHrs>0&&<span style={{color:C.w}}>⏱ {tdHrs.toFixed(1)}h</span>}{td.url&&<a href={td.url} target="_blank" rel="noopener noreferrer" style={{color:C.bl,textDecoration:"none"}}>🔗</a>}</div></div>{pr&&<Badge label={pr.label} color={pr.color}/>}</div>})}</div></Card>})}</div></div>}

  // CLIENT PORTAL
  // CLIENT PORTAL — New version with onboarding, credentials, approvals
  if(user?.role==="client"){
    const cl=clients.find(x=>x.id===user.client_id);
    if(!cl)return<div style={{fontFamily:F,background:C.bg,minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center"}}><style>{CSS}</style><div style={{textAlign:"center"}}><div style={{fontSize:48,marginBottom:16}}>🔒</div><h2 style={{fontFamily:D,color:C.tx}}>Sin acceso</h2><Btn onClick={logout} style={{marginTop:16}}>Salir</Btn></div></div>;

    const cu=clientUsers.find(u=>u.id===user.id);
    const onboardingDone=cu?.onboarding_done;

    const PLATFORMS=[{value:"instagram",label:"Instagram",icon:"📸"},{value:"facebook",label:"Facebook",icon:"📘"},{value:"tiktok",label:"TikTok",icon:"🎵"},{value:"linkedin",label:"LinkedIn",icon:"💼"},{value:"twitter",label:"Twitter/X",icon:"🐦"},{value:"youtube",label:"YouTube",icon:"▶️"},{value:"google_business",label:"Google Business",icon:"📍"},{value:"website",label:"Website (admin)",icon:"🌐"},{value:"email_marketing",label:"Email marketing",icon:"📧"},{value:"hosting",label:"Hosting/Dominio",icon:"🖥️"},{value:"otro",label:"Otro",icon:"🔑"}];

    const saveCred=async()=>{try{if(!credForm.username&&!credForm.password)return;await supabase.from("client_credentials").insert({client_id:cl.id,...credForm});const{data}=await supabase.from("client_credentials").select("*").eq("client_id",cl.id).order("created_at");if(data)setCreds(data);setCredForm({platform:"instagram",username:"",password:"",url:"",notes:""});showToast("Credencial guardada")}catch(e){showToast("Error: "+e.message,"error")}};
    const delCred=async id=>{try{await supabase.from("client_credentials").delete().eq("id",id);setCreds(creds.filter(c=>c.id!==id));showToast("Credencial eliminada")}catch(e){showToast("Error: "+e.message,"error")}};
    const finishOnboarding=async()=>{try{if(cu)await supabase.from("client_users").update({onboarding_done:true}).eq("id",cu.id);setObStep(0)}catch(e){showToast("Error: "+e.message,"error")}};

    // ONBOARDING WIZARD (first time only)
    if(obStep>0)return<div style={{fontFamily:F,background:`radial-gradient(ellipse at 50% 0%,${C.s2},${C.bg})`,minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",padding:20}}><style>{CSS}</style>{toast&&<Toast message={toast.msg} type={toast.type}/>}<div style={{width:"100%",maxWidth:560,animation:"fadeUp .5s ease"}}>
      <div style={{textAlign:"center",marginBottom:28}}><div style={{fontFamily:D,fontSize:36,fontWeight:800,color:C.tx}}>W<span style={{color:C.acc}}>.</span></div><div style={{fontSize:11,color:C.td,letterSpacing:".12em",textTransform:"uppercase"}}>Bienvenido a Wauya Platform</div></div>
      {/* Progress */}
      <div style={{display:"flex",gap:4,marginBottom:20}}>{[1,2,3].map(s=><div key={s} style={{flex:1,height:4,borderRadius:2,background:obStep>=s?C.acc:C.b,transition:"all .3s"}}/>)}</div>
      <Card>
        {obStep===1&&<div>
          <h2 style={{fontFamily:D,fontSize:20,fontWeight:700,color:C.tx,marginBottom:12}}>👋 Hola, {user.name}</h2>
          <p style={{fontSize:13,color:C.tm,lineHeight:1.8,marginBottom:16}}>Bienvenido al portal de <strong style={{color:C.acc}}>{cl.company||cl.name}</strong>. Aquí podrás:</p>
          <div style={{display:"flex",flexDirection:"column",gap:10,marginBottom:20}}>
            {[
              {icon:"🎨",title:"Aprobar la línea gráfica",desc:"Revisas la propuesta visual y la apruebas o pides cambios"},
              {icon:"📅",title:"Aprobar el calendario de contenido",desc:"Cada mes revisas el plan de contenido antes de que se publique"},
              {icon:"📊",title:"Ver métricas y KPIs",desc:"Monitorea el rendimiento de tus redes en tiempo real"},
              {icon:"📦",title:"Descargar entregables",desc:"Todos los archivos que creamos para ti, en un solo lugar"},
              {icon:"🔑",title:"Guardar tus credenciales",desc:"Tus accesos a redes sociales, seguros y organizados"},
            ].map((item,i)=><div key={i} style={{display:"flex",gap:10,padding:"10px 12px",background:C.bg,borderRadius:10,border:`1px solid ${C.b}`}}>
              <span style={{fontSize:20,flexShrink:0}}>{item.icon}</span>
              <div><div style={{fontSize:13,fontWeight:600,color:C.tx}}>{item.title}</div><div style={{fontSize:11,color:C.td,marginTop:2}}>{item.desc}</div></div>
            </div>)}
          </div>
          <Btn onClick={()=>setObStep(2)} sz="lg" style={{width:"100%",justifyContent:"center"}}>Continuar →</Btn>
        </div>}

        {obStep===2&&<div>
          <h2 style={{fontFamily:D,fontSize:20,fontWeight:700,color:C.tx,marginBottom:6}}>🔑 Tus credenciales</h2>
          <p style={{fontSize:12,color:C.tm,marginBottom:16,lineHeight:1.6}}>Para manejar tus redes necesitamos acceso. Ingresa las credenciales de las plataformas que usas. Puedes agregar más después.</p>
          <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10,marginBottom:12}}>
            <Sel label="Plataforma" value={credForm.platform} onChange={e=>setCredForm({...credForm,platform:e.target.value})} options={PLATFORMS}/>
            <Inp label="Usuario / Email" value={credForm.username} onChange={e=>setCredForm({...credForm,username:e.target.value})} placeholder="@usuario o email"/>
          </div>
          <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10,marginBottom:12}}>
            <Inp label="Contraseña" type="password" value={credForm.password} onChange={e=>setCredForm({...credForm,password:e.target.value})} placeholder="••••••"/>
            <Inp label="URL (opcional)" value={credForm.url} onChange={e=>setCredForm({...credForm,url:e.target.value})} placeholder="https://..."/>
          </div>
          <Btn onClick={saveCred} v="secondary" sz="sm" style={{marginBottom:14}}>+ Agregar credencial</Btn>
          {creds.length>0&&<div style={{display:"flex",flexDirection:"column",gap:4,marginBottom:14}}>{creds.map(cr=>{const pl=PLATFORMS.find(p=>p.value===cr.platform)||PLATFORMS[10];return<div key={cr.id} style={{display:"flex",alignItems:"center",gap:8,padding:"8px 10px",background:C.bg,borderRadius:8,border:`1px solid ${C.b}`}}><span style={{fontSize:14}}>{pl.icon}</span><div style={{flex:1}}><span style={{fontSize:12,fontWeight:500,color:C.tx}}>{pl.label}</span><span style={{fontSize:10,color:C.td,marginLeft:6}}>{cr.username}</span></div><span style={{fontSize:10,color:C.g}}>✓ Guardado</span></div>})}</div>}
          <div style={{display:"flex",gap:8}}><Btn onClick={()=>setObStep(1)} v="ghost">← Atrás</Btn><Btn onClick={()=>setObStep(3)} sz="lg" style={{flex:1,justifyContent:"center"}}>{creds.length>0?"Continuar →":"Omitir por ahora →"}</Btn></div>
        </div>}

        {obStep===3&&<div>
          <h2 style={{fontFamily:D,fontSize:20,fontWeight:700,color:C.tx,marginBottom:6}}>✅ ¡Todo listo!</h2>
          <p style={{fontSize:13,color:C.tm,marginBottom:16,lineHeight:1.6}}>Tu portal está configurado. Ahora puedes ver el estado de tu proyecto, aprobar contenido, y acceder a tus archivos.</p>
          <div style={{background:C.bg,borderRadius:10,padding:14,border:`1px solid ${C.b}`,marginBottom:16}}>
            <div style={{fontSize:11,fontWeight:600,color:C.acc,marginBottom:6}}>Resumen:</div>
            <div style={{fontSize:12,color:C.tm,lineHeight:1.8}}>
              <div>👤 Cliente: <strong style={{color:C.tx}}>{cl.company||cl.name}</strong></div>
              <div>🔑 Credenciales guardadas: <strong style={{color:C.tx}}>{creds.length}</strong></div>
              <div>🎯 Servicios: <strong style={{color:C.tx}}>{cl.services||"En definición"}</strong></div>
            </div>
          </div>
          <div style={{display:"flex",gap:8}}><Btn onClick={()=>setObStep(2)} v="ghost">← Atrás</Btn><Btn onClick={finishOnboarding} sz="lg" style={{flex:1,justifyContent:"center"}}>Entrar al portal 🚀</Btn></div>
        </div>}
      </Card>
    </div></div>;

    // MAIN CLIENT PORTAL
    return<div style={{fontFamily:F,background:C.bg,minHeight:"100vh"}}><style>{CSS}</style>{toast&&<Toast message={toast.msg} type={toast.type}/>}
      <div style={{borderBottom:`1px solid ${C.b}`,padding:isMobile?"10px 16px":"14px 28px",display:"flex",justifyContent:"space-between",alignItems:"center"}}><div style={{display:"flex",alignItems:"center",gap:14}}><span style={{fontFamily:D,fontSize:22,fontWeight:800,color:C.tx}}>W<span style={{color:C.acc}}>.</span></span><span style={{color:C.td}}>|</span><span style={{fontSize:13,color:C.tm}}>Portal Cliente</span></div><div style={{display:"flex",alignItems:"center",gap:14}}><span style={{fontSize:12,color:C.tm}}>{user.name}</span><Btn onClick={logout} v="ghost" icon="out" sz="sm">Salir</Btn></div></div>
      <div style={{maxWidth:900,margin:"0 auto",padding:isMobile?"20px 16px":"36px 20px",animation:"fadeUp .4s ease"}}>
        <h1 style={{fontFamily:D,fontSize:isMobile?22:26,fontWeight:700,color:C.tx,marginBottom:6}}>Hola, {user.name} 👋</h1>
        <p style={{fontSize:13,color:C.tm,marginBottom:28}}>Portal de {cl.company||cl.name}</p>

        {/* STATUS BAR */}
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:14}}><h3 style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tx}}>Estado del proyecto</h3><SBadge status={cl.status}/></div><div style={{display:"flex",gap:3,borderRadius:6,overflow:"hidden"}}>{STATUS_OPTS.map((s,i)=><div key={s.value} style={{flex:1,textAlign:"center"}}><div style={{height:5,background:i<=STATUS_OPTS.findIndex(x=>x.value===cl.status)?s.color:C.b,marginBottom:4}}/><div style={{fontSize:8,color:i<=STATUS_OPTS.findIndex(x=>x.value===cl.status)?s.color:C.td}}>{s.icon}</div></div>)}</div></Card>

        {/* BRAND APPROVAL */}
        <Card style={{marginBottom:20}}>
          <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}>
            <h3 style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tx}}>🎨 Línea gráfica</h3>
            {cl.brand_status==="aprobado"?<Badge label="✅ Aprobada" color={C.g}/>:cl.brand_status==="cambios"?<Badge label="✏️ Cambios enviados" color={C.w}/>:<Badge label="⏳ Pendiente" color={C.tm}/>}
          </div>
          {cl.brand_status==="pendiente"&&<p style={{fontSize:12,color:C.td,lineHeight:1.6}}>Tu equipo de Wuaya está preparando la propuesta de línea gráfica. Una vez lista, podrás revisarla y aprobarla aquí.</p>}
          {cl.brand_status==="en_revision"&&<div>
            <p style={{fontSize:12,color:C.tm,marginBottom:12,lineHeight:1.6}}>La propuesta de línea gráfica está lista para tu revisión. Revisa los colores, tipografía y estilo visual propuesto.</p>
            <div style={{display:"flex",gap:8,marginBottom:10}}>
              <Btn onClick={async()=>{await supabase.from("clients").update({brand_status:"aprobado",brand_approved_at:new Date().toISOString()}).eq("id",cl.id);await loadAll();showToast("Línea gráfica aprobada")}} sz="lg" style={{flex:1,justifyContent:"center"}}>✅ Aprobar línea gráfica</Btn>
            </div>
            <Txt value={brandComment} onChange={e=>setBrandComment(e.target.value)} placeholder="¿Quieres pedir algún cambio? Escríbelo aquí..."/>
            {brandComment&&<Btn onClick={async()=>{await supabase.from("clients").update({brand_status:"cambios",brand_comment:brandComment}).eq("id",cl.id);setBrandComment("");await loadAll();showToast("Comentario enviado")}} v="secondary" style={{marginTop:8}}>Enviar cambios</Btn>}
          </div>}
          {cl.brand_status==="aprobado"&&<p style={{fontSize:12,color:C.g}}>✅ Línea gráfica aprobada el {cl.brand_approved_at?new Date(cl.brand_approved_at).toLocaleDateString("es"):""}</p>}
          {cl.brand_status==="cambios"&&<div><p style={{fontSize:12,color:C.w,marginBottom:4}}>✏️ Cambios solicitados:</p><p style={{fontSize:12,color:C.tm,background:C.bg,padding:10,borderRadius:8,border:`1px solid ${C.b}`}}>{cl.brand_comment}</p></div>}
        </Card>

        {/* CALENDAR APPROVAL */}
        {cl.calendar_sheet_id&&<Card style={{marginBottom:20}}>
          <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}>
            <h3 style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tx}}>📅 Calendario de contenido</h3>
            {cl.calendar_status==="aprobado"?<Badge label="✅ Aprobado" color={C.g}/>:cl.calendar_status==="cambios"?<Badge label="✏️ Cambios enviados" color={C.w}/>:<Badge label="⏳ Pendiente" color={C.tm}/>}
          </div>
          <ContentCalendar sheetId={cl.calendar_sheet_id} readOnly/>
          {cl.calendar_status!=="aprobado"&&<div style={{marginTop:14,borderTop:`1px solid ${C.b}`,paddingTop:14}}>
            <div style={{display:"flex",gap:8,marginBottom:10}}>
              <Btn onClick={async()=>{await supabase.from("clients").update({calendar_status:"aprobado",calendar_approved_at:new Date().toISOString()}).eq("id",cl.id);await loadAll();showToast("Calendario aprobado")}} sz="lg" style={{flex:1,justifyContent:"center"}}>✅ Aprobar calendario del mes</Btn>
            </div>
            <Txt value={calComment} onChange={e=>setCalComment(e.target.value)} placeholder="¿Quieres cambiar algo del calendario? Escríbelo aquí..."/>
            {calComment&&<Btn onClick={async()=>{await supabase.from("clients").update({calendar_status:"cambios",calendar_comment:calComment}).eq("id",cl.id);setCalComment("");await loadAll();showToast("Comentario enviado")}} v="secondary" style={{marginTop:8}}>Enviar cambios</Btn>}
          </div>}
          {cl.calendar_status==="aprobado"&&<p style={{fontSize:12,color:C.g,marginTop:10}}>✅ Calendario aprobado el {cl.calendar_approved_at?new Date(cl.calendar_approved_at).toLocaleDateString("es"):""}</p>}
        </Card>}

        {/* KPIs */}
        {cl.sheet_id&&<Card style={{marginBottom:20}}><KPIDashboard sheetId={cl.sheet_id} readOnly/></Card>}

        {/* CREDENTIALS VAULT */}
        <Card style={{marginBottom:20}}>
          <h3 style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tx,marginBottom:14}}>🔑 Mis credenciales</h3>
          <p style={{fontSize:11,color:C.td,marginBottom:12,lineHeight:1.5}}>Tus accesos a redes sociales y herramientas. Solo tú y tu equipo de Wuaya pueden verlos.</p>
          {creds.length>0&&<div style={{display:"flex",flexDirection:"column",gap:6,marginBottom:14}}>{creds.map(cr=>{const pl=PLATFORMS.find(p=>p.value===cr.platform)||PLATFORMS[10];return<div key={cr.id} style={{display:"flex",alignItems:"center",gap:10,padding:"10px 12px",background:C.bg,borderRadius:10,border:`1px solid ${C.b}`}}>
            <span style={{fontSize:18,flexShrink:0}}>{pl.icon}</span>
            <div style={{flex:1,minWidth:0}}>
              <div style={{fontSize:12,fontWeight:600,color:C.tx}}>{pl.label}</div>
              <div style={{fontSize:11,color:C.td}}>👤 {cr.username}{cr.url?` · 🔗 ${cr.url}`:""}</div>
            </div>
            <div style={{display:"flex",gap:4,alignItems:"center"}}>
              <button onClick={()=>setShowPwMap({...showPwMap,[cr.id]:!showPwMap[cr.id]})} style={{background:C.s2,border:`1px solid ${C.b}`,borderRadius:6,padding:"4px 8px",color:C.tm,cursor:"pointer",fontSize:10,fontFamily:F}}>{showPwMap[cr.id]?cr.password:"••••••"}</button>
              <button onClick={()=>delCred(cr.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.r,padding:3}}><Ic n="tr" sz={13}/></button>
            </div>
          </div>})}</div>}
          <div style={{background:C.bg,borderRadius:10,padding:14,border:`1px solid ${C.b}`}}>
            <div style={{fontSize:11,fontWeight:600,color:C.acc,marginBottom:8}}>+ Agregar credencial</div>
            <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8,marginBottom:8}}>
              <Sel value={credForm.platform} onChange={e=>setCredForm({...credForm,platform:e.target.value})} options={PLATFORMS}/>
              <Inp value={credForm.username} onChange={e=>setCredForm({...credForm,username:e.target.value})} placeholder="Usuario / email"/>
            </div>
            <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8,marginBottom:8}}>
              <Inp type="password" value={credForm.password} onChange={e=>setCredForm({...credForm,password:e.target.value})} placeholder="Contraseña"/>
              <Inp value={credForm.url} onChange={e=>setCredForm({...credForm,url:e.target.value})} placeholder="URL (opcional)"/>
            </div>
            <Btn onClick={saveCred} v="secondary" sz="sm">Guardar credencial</Btn>
          </div>
        </Card>

        {/* DELIVERABLES */}
        <Card style={{marginBottom:20}}><h3 style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tx,marginBottom:16}}>📦 Entregables ({clientFiles(cl.id).length})</h3>{clientFiles(cl.id).length===0?<Empty icon="📦" title="Sin entregables" sub="Cuando tu equipo suba archivos, aparecerán aquí"/>:<div style={{display:"flex",flexDirection:"column",gap:8}}>{clientFiles(cl.id).map(fl=><FileCard key={fl.id} file={fl} showDel={false}/>)}</div>}</Card>

        {/* DRIVE */}
        {cl.drive_folder_id&&<Card style={{marginBottom:20}}><h3 style={{fontFamily:D,fontSize:15,fontWeight:600,color:C.tx,marginBottom:14}}>📂 Archivos</h3><DriveFiles ownerType="client" ownerId={cl.id} currentFolderId={cl.drive_folder_id} readOnly/></Card>}

        {/* FOOTER */}
        <div style={{textAlign:"center",padding:"20px 0",fontSize:8,color:C.td}}>© {new Date().getFullYear()} Wuaya Marketing & Communications. Todos los derechos reservados.</div>
      </div>
    </div>;
  }

  // ═══ ADMIN ═══
  const nav=[{key:"dashboard",label:"Dashboard",icon:"dash"},{key:"pipeline",label:"Pipeline",icon:"pipe",cnt:prospects.length},{key:"prospects",label:"Prospectos",icon:"prosp"},{key:"clients",label:"Clientes",icon:"cli",cnt:clients.length},{key:"employees",label:"Empleados",icon:"emp",cnt:employees.length},{key:"timeline",label:"Cronograma",icon:"cal",cnt:todos.filter(t=>t.status!=="completado").length},{key:"gastos",label:"Gastos",icon:"dollar"},{key:"users",label:"Usuarios",icon:"usr"},{key:"settings",label:"Configuración",icon:"lock"}];
  const sp=prospects.find(p=>p.id===selId);const sc=clients.find(x=>x.id===selId);
  const filt=l=>l.filter(x=>(x.name+(x.company||"")+(x.email||"")).toLowerCase().includes(search.toLowerCase()));
  const pad=isMobile?"16px":"24px 32px";const g1=isMobile?"1fr":"1fr 1fr";

  return<div style={{fontFamily:F,background:C.bg,minHeight:"100vh",display:"flex"}}><style>{CSS}</style>
    {saving&&<div style={{position:"fixed",top:12,right:12,background:`linear-gradient(135deg,${C.acc},#D4A00E)`,color:"#060B18",padding:"6px 14px",borderRadius:10,fontSize:11,fontWeight:700,zIndex:2000,animation:"fadeIn .2s",boxShadow:"0 4px 15px rgba(248,186,16,.3)"}}>{syncStatus||"Guardando..."}</div>}
    {toast&&<Toast message={toast.msg} type={toast.type}/>}
    {isMobile&&mobileMenu&&<div onClick={()=>setMM(false)} style={{position:"fixed",inset:0,background:"rgba(0,0,0,.6)",zIndex:898}}/>}
    {/* SIDEBAR */}
    <div style={{width:220,borderRight:`1px solid ${C.b}`,display:"flex",flexDirection:"column",flexShrink:0,background:`linear-gradient(180deg,${C.s},${C.bg})`,position:isMobile?"fixed":"sticky",top:0,height:"100vh",zIndex:899,left:isMobile?(mobileMenu?0:-260):0,transition:"left .3s"}}><div style={{padding:"20px 18px 28px",borderBottom:`1px solid ${C.b}`}}><div style={{fontFamily:D,fontSize:26,fontWeight:800,color:C.tx}}>W<span style={{color:C.acc}}>.</span></div><div style={{fontSize:10,color:C.td,marginTop:1,letterSpacing:".1em",textTransform:"uppercase"}}>Project Manager</div></div><nav style={{flex:1,padding:"12px 8px",display:"flex",flexDirection:"column",gap:1,overflow:"auto"}}>{nav.map(i=><button key={i.key} onClick={()=>{setView(i.key);setSelId(null);setSearch("");setMM(false)}} style={{display:"flex",alignItems:"center",gap:9,padding:"9px 10px",border:"none",borderRadius:10,background:view===i.key?`linear-gradient(135deg,${C.acc}15,${C.acc}08)`:"transparent",color:view===i.key?C.acc:C.tm,fontFamily:F,fontSize:12,fontWeight:view===i.key?600:500,cursor:"pointer",textAlign:"left",width:"100%",transition:"all .2s",borderLeft:view===i.key?`2px solid ${C.acc}`:"2px solid transparent"}}><Ic n={i.icon} sz={17}/>{i.label}{i.cnt>0&&<span style={{marginLeft:"auto",fontSize:10,background:C.bg,padding:"1px 6px",borderRadius:8,color:C.td}}>{i.cnt}</span>}</button>)}</nav><div style={{padding:"14px 10px",borderTop:`1px solid ${C.b}`}}><div style={{display:"flex",alignItems:"center",gap:9,marginBottom:10,padding:"0 4px"}}><div style={{width:28,height:28,borderRadius:7,background:`linear-gradient(135deg,${C.acc},${C.accD})`,display:"flex",alignItems:"center",justifyContent:"center",fontSize:11,fontWeight:700,color:C.bg}}>A</div><div><div style={{fontSize:11,fontWeight:600,color:C.tx}}>Admin</div><div style={{fontSize:9,color:C.td}}>Master</div></div></div><Btn onClick={logout} v="ghost" icon="out" sz="sm" style={{width:"100%",justifyContent:"center"}}>Cerrar Sesión</Btn><div style={{marginTop:10,padding:"8px 4px",borderTop:`1px solid ${C.b}`,textAlign:"center"}}><div style={{fontSize:7,color:C.td,lineHeight:1.5}}>© {new Date().getFullYear()} Wuaya Marketing & Communications. Todos los derechos reservados. Software propietario protegido por leyes de propiedad intelectual. Prohibida su reproducción total o parcial sin autorización expresa.</div></div></div></div>

    <div style={{flex:1,overflow:"auto"}}>
      {/* TOP BAR */}
      <div style={{display:"flex",alignItems:"center",gap:10,padding:isMobile?"10px 16px":"12px 32px",borderBottom:`1px solid ${C.b}`,position:"sticky",top:0,background:`${C.bg}ee`,zIndex:50,backdropFilter:"blur(12px)"}}>
        {isMobile&&<button onClick={()=>setMM(!mobileMenu)} style={{background:"none",border:"none",color:C.tm,cursor:"pointer",padding:4}}><Ic n="menu" sz={22}/></button>}
        <div style={{flex:1,position:"relative"}}><div style={{position:"absolute",left:10,top:"50%",transform:"translateY(-50%)",color:C.td}}><Ic n="srch" sz={14}/></div><input value={globalSearch} onChange={e=>{setGS(e.target.value);setGSOpen(true)}} onFocus={()=>setGSOpen(true)} placeholder="Buscar... (2318K)" style={{width:"100%",background:`linear-gradient(145deg,${C.s},${C.s2})`,border:`1px solid ${C.b}`,borderRadius:10,padding:"8px 10px 8px 32px",color:C.tx,fontSize:12,fontFamily:F,outline:"none",transition:"all .2s"}}/>{gsOpen&&<SearchResults q={globalSearch} prospects={prospects} clients={clients} employees={employees} todos={todos} onSelect={(v,id)=>{setView(v);setSelId(id)}} onClose={()=>{setGSOpen(false);setGS("")}}/>}</div>
        <div style={{position:"relative"}}><button onClick={()=>setNO(!notifOpen)} style={{background:"none",border:"none",color:notifs.length>0?C.w:C.tm,cursor:"pointer",padding:6,position:"relative"}}><Ic n="bell" sz={20}/>{notifs.length>0&&<span style={{position:"absolute",top:2,right:2,width:16,height:16,borderRadius:8,background:C.r,color:"#fff",fontSize:9,fontWeight:700,display:"flex",alignItems:"center",justifyContent:"center"}}>{notifs.length}</span>}</button>{notifOpen&&<div style={{position:"absolute",top:"100%",right:0,width:isMobile?280:360,background:C.s,border:`1px solid ${C.b}`,borderRadius:12,zIndex:100,marginTop:8,maxHeight:400,overflow:"auto"}}><div style={{padding:"12px 16px",borderBottom:`1px solid ${C.b}`,display:"flex",justifyContent:"space-between"}}><span style={{fontFamily:D,fontSize:14,fontWeight:600,color:C.tx}}>Notificaciones ({notifs.length})</span><button onClick={()=>setNO(false)} style={{background:"none",border:"none",color:C.tm,cursor:"pointer"}}>✕</button></div>{notifs.length===0?<div style={{padding:20,textAlign:"center",fontSize:12,color:C.td}}>Todo en orden 🎉</div>:notifs.map(n=><div key={n.id} onClick={()=>{if(n.linkId){setView(n.view);setSelId(n.linkId)}else setView(n.view);setNO(false)}} style={{padding:"10px 16px",borderBottom:`1px solid ${C.b}10`,cursor:"pointer",display:"flex",gap:8}} onMouseEnter={e=>e.currentTarget.style.background=C.bg} onMouseLeave={e=>e.currentTarget.style.background="transparent"}><span style={{fontSize:14,flexShrink:0}}>{n.icon}</span><div style={{fontSize:12,color:C.tx,lineHeight:1.4}}>{n.msg}</div></div>)}</div>}</div>
      </div>
      {gsOpen&&<div onClick={()=>setGSOpen(false)} style={{position:"fixed",inset:0,zIndex:49}}/>}

      <div style={{padding:pad,animation:"fadeIn .25s ease"}}>

      {/* DASHBOARD with profitability + activity */}
      {view==="dashboard"&&<div><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx,marginBottom:4}}>Dashboard</h1><p style={{fontSize:13,color:C.tm,marginBottom:24}}>Vista general</p>
        <div style={{display:"flex",flexWrap:"wrap",gap:12,marginBottom:24}}><Stat label="Prospectos" value={prospects.length} icon="🎯" color={C.w}/><Stat label="Clientes" value={clients.length} icon="💼" color={C.acc}/><Stat label="Tareas" value={todos.filter(t=>t.status!=="completado").length} icon="📋" color={C.bl}/><Stat label="Revenue" value={"$"+totalRevenue.toLocaleString()} icon="💰" color={C.g} sub="Mensual total"/><Stat label="Horas" value={totalHours.toFixed(0)} icon="⏱" color={C.p} sub="Este periodo"/></div>
        <div style={{display:"grid",gridTemplateColumns:g1,gap:16,marginBottom:20}}>
          <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:700,color:C.tx,marginBottom:16}}>Proyectos</h3><div style={{display:"flex",alignItems:"center",gap:20}}><Donut data={stats.sc.map(s=>({value:s.count,color:s.color}))}/><div style={{display:"flex",flexDirection:"column",gap:5}}>{stats.sc.map(s=><div key={s.value} style={{display:"flex",alignItems:"center",gap:7,fontSize:11}}><div style={{width:8,height:8,borderRadius:2,background:s.color}}/><span style={{color:C.tm}}>{s.label}</span><span style={{color:C.tx,fontWeight:700,marginLeft:"auto"}}>{s.count}</span></div>)}</div></div></Card>
          <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:700,color:C.tx,marginBottom:16}}>Rentabilidad por cliente</h3>{clientProfit.filter(c=>c.revenue>0||c.hours>0).length===0?<p style={{fontSize:12,color:C.td}}>Sin datos aún</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{clientProfit.filter(c=>c.revenue>0||c.hours>0).slice(0,5).map(cp=><div key={cp.id} style={{display:"flex",alignItems:"center",gap:8,fontSize:11}}><span style={{flex:1,color:C.tx,fontWeight:500}}>{cp.company||cp.name}</span><span style={{color:C.g}}>💰${cp.revenue}</span><span style={{color:C.p}}>⏱{cp.hours.toFixed(1)}h</span><span style={{color:cp.margin>=50?C.g:cp.margin>=20?C.w:C.r,fontWeight:700}}>{cp.margin}%</span></div>)}</div>}</Card>
        </div>
        {/* CLIENT HEALTH SCORES */}
        {clients.length>0&&<Card style={{marginTop:16}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:700,color:C.tx,marginBottom:16}}>❤️ Salud de clientes</h3><div style={{display:"flex",gap:10,flexWrap:"wrap"}}>{clients.map(cl=>{const hs=getHealthScore(cl);const hc=getHealthColor(hs);return<div key={cl.id} onClick={()=>{setView("clients");setSelId(cl.id)}} style={{background:hc+"10",border:`1px solid ${hc}30`,borderRadius:12,padding:"10px 14px",cursor:"pointer",textAlign:"center",minWidth:80,transition:"all .2s"}} onMouseEnter={e=>e.currentTarget.style.transform="translateY(-2px)"} onMouseLeave={e=>e.currentTarget.style.transform=""}><div style={{fontFamily:D,fontSize:22,fontWeight:800,color:hc}}>{hs}</div><div style={{fontSize:10,fontWeight:500,color:C.tx,marginTop:2}}>{cl.company||cl.name}</div><div style={{fontSize:9,color:hc,marginTop:1}}>{getHealthLabel(hs)}</div></div>})}</div></Card>}
        <div style={{display:"grid",gridTemplateColumns:g1,gap:16}}>
          <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:700,color:C.tx,marginBottom:16}}>Carga por empleado</h3>{stats.el.length===0?<p style={{fontSize:12,color:C.td}}>Sin empleados</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{stats.el.map(e=><div key={e.id} style={{display:"flex",alignItems:"center",gap:8,fontSize:11}}><span style={{flex:1,color:C.tx}}>{e.name}</span><span style={{color:C.bl}}>{e.tc} tareas</span><span style={{color:C.p}}>⏱{e.hrs.toFixed(1)}h</span></div>)}</div>}</Card>
          <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:700,color:C.tx,marginBottom:16}}>Actividad reciente</h3>{activityLog.length===0?<p style={{fontSize:12,color:C.td}}>Sin actividad</p>:<div style={{display:"flex",flexDirection:"column",gap:4}}>{activityLog.slice(0,8).map(a=><div key={a.id} style={{fontSize:11,color:C.tm,padding:"4px 0",borderBottom:`1px solid ${C.b}10`}}><span style={{color:C.tx,fontWeight:500}}>{a.user_name}</span> {a.action} <span style={{color:C.acc}}>{a.entity_name}</span><span style={{color:C.td,marginLeft:8,fontSize:9}}>{new Date(a.created_at).toLocaleDateString("es",{day:"2-digit",month:"short"})}</span></div>)}</div>}</Card>
        </div>
        {/* FINANCIAL REPORT */}
        <Card style={{marginTop:16}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:14}}><div style={{display:"flex",alignItems:"center",gap:8}}><span style={{fontSize:18}}>📊</span><span style={{fontFamily:D,fontSize:14,fontWeight:700,color:C.tx}}>Reporte financiero</span></div><Btn onClick={()=>{const months=["Enero","Febrero","Marzo","Abril","Mayo","Junio","Julio","Agosto","Septiembre","Octubre","Noviembre","Diciembre"];const rows=[["MES","INGRESOS MENSUALES ($)","# CLIENTES ACTIVOS","CLIENTES","HORAS REGISTRADAS","COSTO HORAS ($)","MARGEN ($)"]];months.forEach((m,mi)=>{const activeClients=clients.filter(c=>c.billing_status!=="cancelado"&&c.billing_status!=="pausado");const revenue=activeClients.reduce((s,c)=>s+(parseFloat(c.monthly_fee)||0),0);const hrs=timeEntries.filter(t=>{if(!t.date)return false;const d=new Date(t.date);return d.getMonth()===mi}).reduce((s,t)=>s+(parseFloat(t.hours)||0),0);const cost=timeEntries.filter(t=>{if(!t.date)return false;const d=new Date(t.date);return d.getMonth()===mi}).reduce((s,t)=>{const emp=employees.find(e=>e.id===t.employee_id);return s+((parseFloat(t.hours)||0)*(parseFloat(emp?.hourly_rate)||0))},0);const clientNames=activeClients.map(c=>c.company||c.name).join(" | ");rows.push([m,revenue.toFixed(2),activeClients.length,clientNames,hrs.toFixed(1),cost.toFixed(2),(revenue-cost).toFixed(2)])});rows.push([]);rows.push(["RESUMEN ANUAL"]);rows.push(["Total ingresos",clients.reduce((s,c)=>s+(parseFloat(c.monthly_fee)||0),0)*12]);rows.push(["Total horas",totalHours.toFixed(1)]);rows.push(["Clientes activos",clients.length]);rows.push([]);rows.push(["DETALLE POR CLIENTE","Fee Mensual","Estado cobro","Horas","Costo","Margen","Margen %"]);clientProfit.forEach(cp=>{rows.push([cp.company||cp.name,cp.revenue,cp.billing_status||"activo",cp.hours.toFixed(1),cp.cost.toFixed(2),(cp.revenue-cp.cost).toFixed(2),cp.margin+"%"])});const csv=rows.map(r=>r.map(c=>'"'+String(c||"").replace(/"/g,'""')+'"').join(",")).join("\n");const blob=new Blob(["\uFEFF"+csv],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`Wauya-Finanzas-${todayStr()}.csv`;a.click();URL.revokeObjectURL(url);showToast("Reporte financiero descargado")}} v="secondary" sz="sm" icon="dl">Descargar CSV</Btn></div>
          {/* Monthly summary */}
          <div style={{overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse",fontSize:11,minWidth:500}}><thead><tr>{["Mes","Ingresos","Horas","Costo","Margen"].map(h=><th key={h} style={{padding:"6px 8px",textAlign:h==="Mes"?"left":"right",color:C.tm,fontWeight:600,borderBottom:`2px solid ${C.acc}`,fontSize:10,textTransform:"uppercase"}}>{h}</th>)}</tr></thead><tbody>{["Ene","Feb","Mar","Abr","May","Jun","Jul","Ago","Sep","Oct","Nov","Dic"].map((m,mi)=>{const revenue=clients.filter(c=>c.billing_status!=="cancelado"&&c.billing_status!=="pausado").reduce((s,c)=>s+(parseFloat(c.monthly_fee)||0),0);const hrs=timeEntries.filter(t=>{if(!t.date)return false;const d=new Date(t.date);return d.getMonth()===mi}).reduce((s,t)=>s+(parseFloat(t.hours)||0),0);const cost=timeEntries.filter(t=>{if(!t.date)return false;const d=new Date(t.date);return d.getMonth()===mi}).reduce((s,t)=>{const emp=employees.find(e=>e.id===t.employee_id);return s+((parseFloat(t.hours)||0)*(parseFloat(emp?.hourly_rate)||0))},0);const margin=revenue-cost;const isCurrent=mi===new Date().getMonth();return<tr key={mi} style={{background:isCurrent?C.acc+"08":"transparent"}}><td style={{padding:"6px 8px",fontWeight:isCurrent?700:400,color:isCurrent?C.acc:C.tx}}>{m}</td><td style={{padding:"6px 8px",textAlign:"right",color:C.g,fontWeight:600}}>{revenue>0?"$"+revenue.toLocaleString():""}</td><td style={{padding:"6px 8px",textAlign:"right",color:hrs>0?C.p:C.td}}>{hrs>0?hrs.toFixed(1)+"h":""}</td><td style={{padding:"6px 8px",textAlign:"right",color:cost>0?C.w:C.td}}>{cost>0?"$"+Math.round(cost):""}</td><td style={{padding:"6px 8px",textAlign:"right",fontWeight:600,color:margin>0?C.g:margin<0?C.r:C.td}}>{revenue>0?"$"+Math.round(margin):""}</td></tr>})}</tbody></table></div>
        </Card>
        <Card style={{marginTop:16}}><Expenses sheetId={localStorage.getItem("wauya_expense_sheet")||null} onLink={async sid=>{localStorage.setItem("wauya_expense_sheet",sid)}} onUnlink={()=>{localStorage.removeItem("wauya_expense_sheet")}} compact/></Card>
      </div>}

      {/* PIPELINE */}
      {view==="pipeline"&&<div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20,flexWrap:"wrap",gap:8}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>Pipeline</h1><div style={{display:"flex",gap:8}}><Btn onClick={()=>setModal("sync_drive")} v="secondary" icon="sync" sz={isMobile?"sm":"md"}>Sync</Btn><Btn onClick={()=>setModal("add_prospect")} icon="plus" sz={isMobile?"sm":"md"}>Nuevo</Btn></div></div><div style={{overflowX:"auto"}}><Pipeline prospects={prospects} onMove={moveProspect} onSelect={id=>{setSelId(id);setView("prospects")}}/></div></div>}

      {/* PROSPECTS */}
      {view==="prospects"&&!selId&&<div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20,flexWrap:"wrap",gap:8}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>Prospectos</h1><div style={{display:"flex",gap:8}}><Btn onClick={()=>setModal("sync_drive")} v="secondary" icon="sync" sz={isMobile?"sm":"md"}>Sync</Btn><Btn onClick={()=>setModal("add_prospect")} icon="plus" sz={isMobile?"sm":"md"}>Nuevo</Btn></div></div><div style={{position:"relative",marginBottom:18}}><div style={{position:"absolute",left:11,top:"50%",transform:"translateY(-50%)",color:C.td}}><Ic n="srch" sz={15}/></div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar..." style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:10,padding:"9px 12px 9px 34px",color:C.tx,fontSize:12,fontFamily:F,outline:"none"}}/></div>{filt(prospects).length===0?<Empty icon="🎯" title="Sin prospectos" sub="Crea tu primer prospecto o sincroniza desde Drive" action={()=>setModal("add_prospect")} actionLabel="+ Nuevo prospecto"/>:<div style={{display:"grid",gridTemplateColumns:isMobile?"1fr":"repeat(auto-fill,minmax(280px,1fr))",gap:12}}>{filt(prospects).map(p=><Card key={p.id} hover onClick={()=>setSelId(p.id)}><div style={{display:"flex",justifyContent:"space-between",marginBottom:8}}><div><div style={{fontFamily:D,fontSize:14,fontWeight:600,color:C.tx}}>{p.company||p.name}</div><div style={{fontSize:11,color:C.tm,marginTop:1}}>{p.name}</div></div>{p.drive_folder_id&&<span style={{fontSize:10,color:C.g}}>📁</span>}</div>{p.email&&<div style={{fontSize:11,color:C.td}}>✉ {p.email}</div>}</Card>)}</div>}</div>}

      {/* PROSPECT DETAIL with templates */}
      {view==="prospects"&&selId&&sp&&<div><button onClick={()=>setSelId(null)} style={{display:"flex",alignItems:"center",gap:5,background:"none",border:"none",color:C.tm,cursor:"pointer",fontSize:12,marginBottom:18,padding:0,fontFamily:F}}><Ic n="back" sz={15}/>Volver</button><div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:24,flexWrap:"wrap",gap:8}}><div><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>{sp.company||sp.name}</h1><p style={{fontSize:12,color:C.tm,marginTop:2}}>{sp.name} · {sp.email}</p></div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><Btn onClick={()=>setModal({type:"onboarding",prospect:sp})} sz="sm">Convertir a Cliente</Btn><Btn onClick={()=>setModal({type:"apply_template",targetType:"prospect",targetId:sp.id})} v="secondary" sz="sm" icon="copy">Template</Btn><Btn onClick={()=>delProspect(sp.id)} v="danger" sz="sm" icon="tr"/></div></div>
        <div style={{display:"grid",gridTemplateColumns:g1,gap:14,marginBottom:20}}><Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:10}}>Info</h3>{sp.phone&&<div style={{fontSize:12,color:C.tm,marginBottom:4}}>📱 {sp.phone}</div>}{sp.services&&<div style={{fontSize:12,color:C.tm}}>🎯 {sp.services}</div>}</Card><Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:10}}>Notas</h3><textarea value={sp.notes||""} onChange={e=>{const v=e.target.value;setProspects(pr=>pr.map(x=>x.id===sp.id?{...x,notes:v}:x));autoSave("prospects",sp.id,{notes:v})}} placeholder="Notas..." style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:8,padding:8,color:C.tx,fontSize:12,fontFamily:F,outline:"none",resize:"vertical",minHeight:70}}/></Card></div>
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>Tareas ({todos.filter(t=>t.prospect_id===sp.id).length})</h3><Btn onClick={()=>setModal({type:"add_todo_global",defaults:{category:"propuesta",prospect_id:sp.id}})} v="secondary" sz="sm" icon="plus">Nueva</Btn></div>{todos.filter(t=>t.prospect_id===sp.id).length===0?<p style={{fontSize:11,color:C.td}}>Sin tareas — usa el botón Template para crear varias</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{todos.filter(t=>t.prospect_id===sp.id).map(td=><TaskRow key={td.id} td={td} employees={employees} clients={clients} prospects={prospects} onStatusChange={(id,u)=>updTodo(id,u)} onEdit={t=>setModal({type:"edit_todo",todo:t})} onDelete={delTodo} onLogTime={t=>setModal({type:"log_time",task:t})} onDuplicate={t=>dupTodo(t)}/>)}</div>}</Card>
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:14}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>Archivos ({prospFiles(sp.id).length})</h3><Btn onClick={()=>setModal("upload_pf")} v="secondary" sz="sm" icon="plus">Subir</Btn></div>{prospFiles(sp.id).length===0?<Empty icon="📁" title="Sin archivos"/>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{prospFiles(sp.id).map(fl=><FileCard key={fl.id} file={fl} onDelete={deleteFile}/>)}</div>}</Card>
        <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:14}}>📂 Google Drive</h3><DriveFiles ownerType="prospect" ownerId={sp.id} currentFolderId={sp.drive_folder_id} onUpdate={loadAll}/></Card>
      </div>}

      {/* CLIENTS LIST */}
      {view==="clients"&&!selId&&<div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20,flexWrap:"wrap",gap:8}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>Clientes</h1><Btn onClick={()=>setModal("add_client")} icon="plus" sz={isMobile?"sm":"md"}>Nuevo</Btn></div><div style={{position:"relative",marginBottom:18}}><div style={{position:"absolute",left:11,top:"50%",transform:"translateY(-50%)",color:C.td}}><Ic n="srch" sz={15}/></div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar..." style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:10,padding:"9px 12px 9px 34px",color:C.tx,fontSize:12,fontFamily:F,outline:"none"}}/></div>{filt(clients).length===0?<Empty icon="💼" title="Sin clientes" sub="Convierte un prospecto o crea un cliente directo" action={()=>setModal("add_client")} actionLabel="+ Nuevo cliente"/>:<div style={{display:"grid",gridTemplateColumns:isMobile?"1fr":"repeat(auto-fill,minmax(300px,1fr))",gap:12}}>{filt(clients).map(cl=>{const cp=clientProfit.find(x=>x.id===cl.id);return<Card key={cl.id} hover onClick={()=>setSelId(cl.id)}><div style={{display:"flex",justifyContent:"space-between",marginBottom:8}}><div><div style={{fontFamily:D,fontSize:14,fontWeight:600,color:C.tx}}>{cl.company||cl.name}</div><div style={{fontSize:11,color:C.tm,marginTop:1}}>{cl.name}</div></div><div style={{display:"flex",gap:6,alignItems:"flex-start"}}>{(()=>{const hs=getHealthScore(cl);return<span style={{fontSize:10,fontWeight:700,padding:"2px 8px",borderRadius:10,background:getHealthColor(hs)+"18",color:getHealthColor(hs)}}>{hs}</span>})()}<SBadge status={cl.status}/></div></div>{cl.services&&<div style={{fontSize:11,color:C.td,marginBottom:6}}>🎯 {cl.services}</div>}<div style={{display:"flex",gap:10,fontSize:10,color:C.td,flexWrap:"wrap"}}><span><Ic n="todo" sz={11}/>{todos.filter(td=>td.client_id===cl.id).length}</span>{cl.monthly_fee>0&&<span style={{color:C.g}}>💰${cl.monthly_fee}/mes</span>}{cp&&cp.hours>0&&<span style={{color:C.bl}}>⏱{cp.hours.toFixed(1)}h</span>}</div></Card>})}</div>}</div>}

      {/* CLIENT DETAIL with templates + time */}
      {view==="clients"&&selId&&sc&&<div>
        <button onClick={()=>setSelId(null)} style={{display:"flex",alignItems:"center",gap:5,background:"none",border:"none",color:C.tm,cursor:"pointer",fontSize:12,marginBottom:18,padding:0,fontFamily:F}}><Ic n="back" sz={15}/>Volver</button>
        <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:24,flexWrap:"wrap",gap:8}}><div><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx,cursor:"text",borderBottom:`1px dashed ${C.b}`,paddingBottom:2}} contentEditable suppressContentEditableWarning onBlur={e=>{const v=e.target.innerText.trim();if(v&&v!==(sc.company||sc.name))updClient(sc.id,{company:v})}}>{sc.company||sc.name}</h1><p style={{fontSize:12,color:C.tm,marginTop:2}}><span contentEditable suppressContentEditableWarning style={{borderBottom:`1px dashed ${C.b}`,cursor:"text"}} onBlur={e=>{const v=e.target.innerText.trim();if(v&&v!==sc.name)updClient(sc.id,{name:v})}}>{sc.name}</span> · {sc.email}</p></div><div style={{display:"flex",gap:8,alignItems:"center",flexWrap:"wrap"}}><Btn onClick={()=>handleQBR(sc)} v="secondary" sz="sm">📊 QBR</Btn><Btn onClick={()=>setModal({type:"apply_template",targetType:"client",targetId:sc.id})} v="secondary" sz="sm" icon="copy">Template</Btn><Sel value={sc.status} onChange={e=>updClient(sc.id,{status:e.target.value})} options={STATUS_OPTS} style={{fontSize:11,padding:"6px 10px"}}/><Btn onClick={()=>delClient(sc.id)} v="danger" sz="sm" icon="tr"/></div></div>
        {/* Profitability bar */}
        {(()=>{const cp=clientProfit.find(x=>x.id===sc.id);if(!cp)return null;return(cp.revenue>0||cp.hours>0)&&<div style={{display:"flex",gap:12,marginBottom:20,flexWrap:"wrap"}}><Stat label="Revenue" value={"$"+cp.revenue} icon="💰" color={C.g}/><Stat label="Horas" value={cp.hours.toFixed(1)} icon="⏱" color={C.p}/><Stat label="Costo" value={"$"+cp.cost.toFixed(0)} icon="📉" color={C.w}/><Stat label="Margen" value={cp.margin+"%"} icon="📊" color={cp.margin>=50?C.g:cp.margin>=20?C.w:C.r}/></div>})()}
        <div style={{display:"grid",gridTemplateColumns:g1,gap:14,marginBottom:20}}>
          <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:10}}>Info</h3>{sc.phone&&<div style={{fontSize:12,color:C.tm,marginBottom:4}}>📱 {sc.phone}</div>}{sc.services&&<div style={{fontSize:12,color:C.tm}}>🎯 {sc.services}</div>}</Card>
          <Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:10}}>Notas</h3><textarea value={sc.notes||""} onChange={e=>{const v=e.target.value;setClients(cl=>cl.map(x=>x.id===sc.id?{...x,notes:v}:x));autoSave("clients",sc.id,{notes:v})}} placeholder="Notas..." style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:8,padding:8,color:C.tx,fontSize:12,fontFamily:F,outline:"none",resize:"vertical",minHeight:70}}/></Card>
        </div>
        {/* BILLING */}
        <Card style={{marginBottom:20}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:10}}><Ic n="dollar" sz={14}/> Facturación</h3><div style={{display:"grid",gridTemplateColumns:"1fr 1fr 1fr",gap:10}}><div><label style={{fontSize:10,color:C.tm}}>Fee mensual ($)</label><input type="number" value={sc.monthly_fee||0} onChange={e=>{const v=e.target.value;setClients(cl=>cl.map(x=>x.id===sc.id?{...x,monthly_fee:v}:x));autoSave("clients",sc.id,{monthly_fee:parseFloat(v)||0})}} style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:6,padding:"6px 8px",color:C.tx,fontSize:13,fontFamily:F,outline:"none",marginTop:4}}/></div><div><label style={{fontSize:10,color:C.tm}}>Día de cobro</label><input type="number" min="1" max="31" value={sc.billing_day||1} onChange={e=>{const v=e.target.value;setClients(cl=>cl.map(x=>x.id===sc.id?{...x,billing_day:v}:x));autoSave("clients",sc.id,{billing_day:parseInt(v)||1})}} style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:6,padding:"6px 8px",color:C.tx,fontSize:13,fontFamily:F,outline:"none",marginTop:4}}/></div><div><label style={{fontSize:10,color:C.tm}}>Estado</label><select value={sc.billing_status||"activo"} onChange={e=>{updClient(sc.id,{billing_status:e.target.value});showToast("Estado de cobro actualizado")}} style={{width:"100%",background:C.bg,border:`1px solid ${C.b}`,borderRadius:6,padding:"6px 8px",color:C.tx,fontSize:12,fontFamily:F,outline:"none",marginTop:4}}>{BILL_STATUS.map(b=><option key={b.value} value={b.value}>{b.label}</option>)}</select></div></div></Card>
        {/* BRAND APPROVAL CONTROL */}
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:10}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>🎨 Línea gráfica</h3><div style={{display:"flex",gap:6}}>{["pendiente","en_revision","aprobado"].map(st=><button key={st} onClick={()=>{updClient(sc.id,{brand_status:st});showToast("Estado actualizado")}} style={{padding:"4px 10px",borderRadius:6,border:`1px solid ${sc.brand_status===st?C.acc:C.b}`,background:sc.brand_status===st?C.acc+"15":"transparent",color:sc.brand_status===st?C.acc:C.td,fontSize:10,fontWeight:600,cursor:"pointer",fontFamily:F}}>{st==="pendiente"?"⏳ Pendiente":st==="en_revision"?"👁️ En revisión":"✅ Aprobada"}</button>)}</div></div>{sc.brand_status==="cambios"&&<div style={{background:C.bg,borderRadius:8,padding:10,border:`1px solid ${C.w}30`}}><div style={{fontSize:10,fontWeight:600,color:C.w,marginBottom:4}}>Cliente pidió cambios:</div><div style={{fontSize:12,color:C.tm}}>{sc.brand_comment||"Sin comentario"}</div></div>}</Card>

        {/* CALENDAR APPROVAL CONTROL */}
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:10}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>📅 Aprobación calendario</h3><div style={{display:"flex",gap:6}}>{["pendiente","aprobado"].map(st=><button key={st} onClick={()=>{updClient(sc.id,{calendar_status:st});showToast("Estado actualizado")}} style={{padding:"4px 10px",borderRadius:6,border:`1px solid ${sc.calendar_status===st?C.acc:C.b}`,background:sc.calendar_status===st?C.acc+"15":"transparent",color:sc.calendar_status===st?C.acc:C.td,fontSize:10,fontWeight:600,cursor:"pointer",fontFamily:F}}>{st==="pendiente"?"⏳ Pendiente":"✅ Aprobado"}</button>)}</div></div>{sc.calendar_status==="cambios"&&<div style={{background:C.bg,borderRadius:8,padding:10,border:`1px solid ${C.w}30`}}><div style={{fontSize:10,fontWeight:600,color:C.w,marginBottom:4}}>Cliente pidió cambios:</div><div style={{fontSize:12,color:C.tm}}>{sc.calendar_comment||"Sin comentario"}</div></div>}</Card>

        {/* CLIENT CREDENTIALS (admin view) */}
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:10}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>🔑 Credenciales del cliente</h3></div><ClientCreds clientId={sc.id}/></Card>
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>Usuarios ({clientUsers.filter(u=>u.client_id===sc.id).length})</h3><Btn onClick={()=>setModal("add_cu")} v="secondary" sz="sm" icon="plus">Crear</Btn></div>{clientUsers.filter(u=>u.client_id===sc.id).length===0?<p style={{fontSize:11,color:C.td}}>Sin usuarios</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{clientUsers.filter(u=>u.client_id===sc.id).map(u=><div key={u.id} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"8px 12px",background:C.bg,borderRadius:8,border:`1px solid ${C.b}`}}><div><div style={{fontSize:12,fontWeight:500,color:C.tx}}>{u.name}</div><div style={{fontSize:10,color:C.td}}>{u.email} · 🔑 {u.password}</div></div><button onClick={()=>delCU(u.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.r,padding:4}}><Ic n="tr" sz={13}/></button></div>)}</div>}</Card>
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>Tareas ({todos.filter(td=>td.client_id===sc.id).length})</h3><Btn onClick={()=>setModal({type:"add_todo_global",defaults:{category:"cliente",client_id:sc.id}})} v="secondary" sz="sm" icon="plus">Nueva</Btn></div>{todos.filter(td=>td.client_id===sc.id).length===0?<p style={{fontSize:11,color:C.td}}>Sin tareas</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{todos.filter(td=>td.client_id===sc.id).map(td=><TaskRow key={td.id} td={td} employees={employees} clients={clients} prospects={prospects} onStatusChange={(id,u)=>updTodo(id,u)} onEdit={t=>setModal({type:"edit_todo",todo:t})} onDelete={delTodo} onLogTime={t=>setModal({type:"log_time",task:t})} onDuplicate={t=>dupTodo(t)}/>)}</div>}</Card>
        <Card style={{marginBottom:20}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx}}>Entregables ({clientFiles(sc.id).length})</h3><Btn onClick={()=>setModal("upload_del")} v="secondary" sz="sm" icon="plus">Subir</Btn></div>{clientFiles(sc.id).length===0?<Empty icon="📦" title="Sin entregables"/>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{clientFiles(sc.id).map(fl=><FileCard key={fl.id} file={fl} onDelete={deleteFile}/>)}</div>}</Card>
        <Card style={{marginBottom:20}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:14}}>📅 Calendario</h3><ContentCalendar sheetId={sc.calendar_sheet_id} onLink={async sid=>{await dbUpd("clients",sc.id,{calendar_sheet_id:sid})}} onUnlink={async()=>{await dbUpd("clients",sc.id,{calendar_sheet_id:null})}}/></Card>
        <Card style={{marginBottom:20}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:14}}>📂 Google Drive</h3><DriveFiles ownerType="client" ownerId={sc.id} currentFolderId={sc.drive_folder_id} onUpdate={loadAll}/></Card>
        <Card><KPIDashboard sheetId={sc.sheet_id} onLink={async sid=>{await dbUpd("clients",sc.id,{sheet_id:sid})}} onUnlink={async()=>{await dbUpd("clients",sc.id,{sheet_id:null})}}/></Card>
      </div>}

      {/* EMPLOYEES with hourly rate */}
      {view==="employees"&&<div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>Empleados</h1><Btn onClick={()=>setModal("add_emp")} icon="plus" sz={isMobile?"sm":"md"}>Nuevo</Btn></div>{employees.length===0?<Empty icon="👤" title="Sin empleados" sub="Agrega a tu equipo para asignarles tareas" action={()=>setModal("add_emp")} actionLabel="+ Nuevo empleado"/>:<div style={{display:"grid",gridTemplateColumns:isMobile?"1fr":"repeat(auto-fill,minmax(300px,1fr))",gap:12}}>{employees.map(e=>{const tasks=todos.filter(td=>td.assigned_to===e.id);const done=tasks.filter(td=>td.status==="completado").length;const hrs=timeEntries.filter(t=>t.employee_id===e.id).reduce((s,t)=>s+(parseFloat(t.hours)||0),0);return<Card key={e.id}><div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:8}}><div style={{display:"flex",gap:10,alignItems:"center"}}><div style={{width:36,height:36,borderRadius:10,background:`linear-gradient(135deg,${C.p},${C.bl})`,display:"flex",alignItems:"center",justifyContent:"center",fontSize:14,fontWeight:700,color:"#fff"}}>{e.name?.charAt(0)?.toUpperCase()}</div><div><div style={{fontFamily:D,fontSize:14,fontWeight:600,color:C.tx}}>{e.name}</div><div style={{fontSize:10,color:C.td}}>{e.role||"Empleado"}{e.hourly_rate>0&&<span style={{color:C.g}}> · ${e.hourly_rate}/h</span>}</div></div></div><button onClick={()=>delEmp(e.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.r,padding:3}}><Ic n="tr" sz={14}/></button></div><div style={{fontSize:11,color:C.tm,marginBottom:8}}>✉ {e.email} · 🔑 {e.password}</div><div style={{display:"flex",gap:8,fontSize:10,color:C.td,flexWrap:"wrap"}}><span>{tasks.length} tareas</span><span style={{color:C.g}}>· {done} completadas</span><span style={{color:C.p}}>⏱ {hrs.toFixed(1)}h registradas</span></div>{tasks.length>0&&<div style={{marginTop:6,background:C.bg,borderRadius:6,overflow:"hidden",height:4,display:"flex"}}><div style={{width:`${(done/tasks.length)*100}%`,background:C.g,transition:"width .3s"}}/></div>}<div style={{marginTop:8}}><label style={{fontSize:10,color:C.tm}}>Rate por hora ($)</label><input type="number" value={e.hourly_rate||0} onChange={ev=>{const v=ev.target.value;setEmployees(em=>em.map(x=>x.id===e.id?{...x,hourly_rate:v}:x));autoSave("employees",e.id,{hourly_rate:parseFloat(v)||0})}} style={{width:80,background:C.bg,border:`1px solid ${C.b}`,borderRadius:6,padding:"4px 8px",color:C.tx,fontSize:12,fontFamily:F,outline:"none",marginTop:4,marginLeft:8}}/></div></Card>})}</div>}</div>}

      {/* CRONOGRAMA */}
      {view==="timeline"&&<div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20,flexWrap:"wrap",gap:8}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>Cronograma</h1><div style={{display:"flex",gap:6,flexWrap:"wrap"}}><Btn onClick={()=>setModal({type:"dup_period"})} v="secondary" sz={isMobile?"sm":"md"} icon="copy">Duplicar periodo</Btn><Btn onClick={()=>setModal({type:"add_todo_global",defaults:{}})} icon="plus" sz={isMobile?"sm":"md"}>Nueva tarea</Btn></div></div><div style={{display:"flex",gap:12,marginBottom:20,flexWrap:"wrap"}}>{CATS.map(cat=>{const pending=todos.filter(t=>(t.category||"cliente")===cat.value&&t.status!=="completado").length;return<div key={cat.value} style={{background:C.s,borderRadius:10,border:`1px solid ${C.b}`,padding:"10px 16px",display:"flex",alignItems:"center",gap:8}}><span style={{fontSize:16}}>{cat.icon}</span><div><div style={{fontSize:10,color:C.tm}}>{cat.label}s</div><div style={{fontSize:18,fontWeight:800,color:cat.color,fontFamily:D}}>{pending}</div></div></div>})}</div>{CATS.map(cat=>{const catTodos=todos.filter(t=>(t.category||"cliente")===cat.value);if(!catTodos.length)return null;const groups={};catTodos.forEach(td=>{let key,name;if(td.client_id){const cl=clients.find(x=>x.id===td.client_id);key=td.client_id;name=cl?.company||"Cliente"}else if(td.prospect_id){const pr=prospects.find(x=>x.id===td.prospect_id);key=td.prospect_id;name=pr?.company||"Prospecto"}else{key="interno";name="Interno"}if(!groups[key])groups[key]={name,todos:[]};groups[key].todos.push(td)});return<div key={cat.value} style={{marginBottom:24}}><h3 style={{fontFamily:D,fontSize:16,fontWeight:700,color:cat.color,marginBottom:12}}>{cat.icon} {cat.label}s</h3>{Object.entries(groups).map(([k,{name,todos:tds}])=><div key={k} style={{marginBottom:16}}><div style={{fontSize:13,fontWeight:600,color:C.tx,marginBottom:8,paddingLeft:4}}>{name}</div><Timeline tasks={tds} employees={employees}/></div>)}</div>})}{todos.length===0&&<Empty icon="📅" title="Sin tareas" sub="Crea tareas para tus clientes, propuestas o internas" action={()=>setModal({type:"add_todo_global",defaults:{}})} actionLabel="+ Nueva tarea"/>}</div>}

      {/* USERS */}
      {/* GASTOS */}
      {view==="gastos"&&<div><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx,marginBottom:4}}>Gastos</h1><p style={{fontSize:13,color:C.tm,marginBottom:20}}>Control de gastos desde Google Sheets</p><Card><Expenses sheetId={localStorage.getItem("wauya_expense_sheet")||null} onLink={async sid=>{localStorage.setItem("wauya_expense_sheet",sid)}} onUnlink={()=>{localStorage.removeItem("wauya_expense_sheet")}}/></Card></div>}

      {view==="users"&&<div><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20}}><h1 style={{fontFamily:D,fontSize:isMobile?20:24,fontWeight:700,color:C.tx}}>Usuarios</h1><Btn onClick={()=>{const rows=[["Rol","Nombre","Email","Contraseña","Empresa"]];rows.push(["Master Admin",user?.name||"Admin",user?.email||"","(ver configuración)",""]);employees.forEach(e=>rows.push(["Empleado",e.name,e.email,e.password,e.role||""]));clientUsers.forEach(u=>{const cl=clients.find(x=>x.id===u.client_id);rows.push(["Cliente",u.name,u.email,u.password,cl?.company||""])});const csv=rows.map(r=>r.map(c=>'"'+String(c||"").replace(/"/g,'""')+'"').join(",")).join("\n");const blob=new Blob(["\uFEFF"+csv],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`Wauya-Usuarios-${todayStr()}.csv`;a.click();URL.revokeObjectURL(url);showToast("Usuarios descargados")}} v="secondary" sz={isMobile?"sm":"md"} icon="dl">Descargar CSV</Btn></div><Card style={{marginBottom:16}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:700,color:C.acc,marginBottom:12}}>Master</h3><div style={{display:"flex",alignItems:"center",gap:12,padding:"8px 12px",background:C.bg,borderRadius:8,border:`1px solid ${C.b}`,flexWrap:"wrap"}}><div style={{width:30,height:30,borderRadius:8,background:`linear-gradient(135deg,${C.acc},${C.accD})`,display:"flex",alignItems:"center",justifyContent:"center",fontSize:12,fontWeight:700,color:C.bg}}>A</div><div style={{flex:1}}><div style={{fontSize:12,fontWeight:500,color:C.tx}}>{user?.name||"Admin"}</div><div style={{fontSize:10,color:C.td}}>{user?.email}</div></div><Badge label="MASTER" color={C.acc}/><Btn onClick={()=>setView("settings")} v="ghost" sz="sm" icon="edit">Editar</Btn></div></Card><Card style={{marginBottom:16}}><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:12}}>Empleados ({employees.length})</h3>{employees.length===0?<p style={{fontSize:11,color:C.td}}>Sin empleados</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{employees.map(e=><div key={e.id} style={{display:"flex",alignItems:"center",gap:10,padding:"8px 12px",background:C.bg,borderRadius:8,border:`1px solid ${C.b}`,flexWrap:"wrap"}}><div style={{width:26,height:26,borderRadius:6,background:C.pBg,display:"flex",alignItems:"center",justifyContent:"center",fontSize:11,fontWeight:600,color:C.p}}>{e.name?.charAt(0)?.toUpperCase()}</div><div style={{flex:1}}><div style={{fontSize:12,fontWeight:500,color:C.tx}}>{e.name}</div><div style={{fontSize:10,color:C.td}}>{e.email} · 🔑 {e.password}</div></div><Badge label="EMPLEADO" color={C.p}/><Btn onClick={()=>setModal({type:"edit_user",table:"employees",user:e})} v="ghost" sz="sm" icon="edit"/></div>)}</div>}</Card><Card><h3 style={{fontFamily:D,fontSize:13,fontWeight:600,color:C.tx,marginBottom:12}}>Clientes ({clientUsers.length})</h3>{clientUsers.length===0?<p style={{fontSize:11,color:C.td}}>Sin usuarios</p>:<div style={{display:"flex",flexDirection:"column",gap:6}}>{clientUsers.map(u=>{const cl=clients.find(x=>x.id===u.client_id);return<div key={u.id} style={{display:"flex",alignItems:"center",gap:10,padding:"8px 12px",background:C.bg,borderRadius:8,border:`1px solid ${C.b}`,flexWrap:"wrap"}}><div style={{width:26,height:26,borderRadius:6,background:C.blBg,display:"flex",alignItems:"center",justifyContent:"center",fontSize:11,fontWeight:600,color:C.bl}}>{u.name?.charAt(0)?.toUpperCase()}</div><div style={{flex:1}}><div style={{fontSize:12,fontWeight:500,color:C.tx}}>{u.name}</div><div style={{fontSize:10,color:C.td}}>{u.email} · {cl?.company||"—"} · 🔑 {u.password}</div></div><Badge label="CLIENTE" color={C.bl}/><Btn onClick={()=>setModal({type:"edit_user",table:"client_users",user:u})} v="ghost" sz="sm" icon="edit"/></div>})}</div>}</Card></div>}

      {/* SETTINGS */}
      {view==="settings"&&<SettingsView user={user} onSave={async upd=>{setSaving(true);await supabase.from("admin_users").update(upd).eq("id",user.id);const nu={...user,...upd};delete nu.password;setUser(nu);localStorage.setItem("wauya_user",JSON.stringify(nu));setSaving(false);showToast("Configuración actualizada")}} onBack={()=>setView("users")}/>}

    </div></div>

    {/* MODALS */}
    {modal==="sync_drive"&&<Mod title="Sincronizar desde Drive" onClose={()=>setModal(null)}><SyncForm onSync={syncFromDrive} onCancel={()=>setModal(null)} saving={saving} syncStatus={syncStatus}/></Mod>}
    {modal==="add_prospect"&&<Mod title="Nuevo Prospecto" onClose={()=>setModal(null)}><FP onDone={addProspect} onX={()=>setModal(null)}/></Mod>}
    {modal==="add_client"&&<Mod title="Nuevo Cliente" onClose={()=>setModal(null)}><FC onDone={addClient} onX={()=>setModal(null)}/></Mod>}
    {modal==="add_cu"&&sc&&<Mod title={`Usuario - ${sc.company||sc.name}`} onClose={()=>setModal(null)}><FCU onDone={u=>addCU(sc.id,u)} onX={()=>setModal(null)}/></Mod>}
    {modal==="add_emp"&&<Mod title="Nuevo Empleado" onClose={()=>setModal(null)}><FE onDone={addEmp} onX={()=>setModal(null)}/></Mod>}
    {modal?.type==="add_todo_global"&&<Mod title="Nueva Tarea" onClose={()=>setModal(null)} w={600}><FTGlobal clients={clients} prospects={prospects} emps={employees} defaults={modal.defaults} onDone={addTodo} onX={()=>setModal(null)}/></Mod>}
    {modal?.type==="edit_todo"&&<Mod title="Editar Tarea" onClose={()=>setModal(null)} w={600}><FTGlobal clients={clients} prospects={prospects} emps={employees} init={modal.todo} onDone={async u=>{await updTodo(modal.todo.id,u);setModal(null)}} onX={()=>setModal(null)} isEdit/></Mod>}
    {modal?.type==="log_time"&&<Mod title="Registrar horas" onClose={()=>setModal(null)} w={400}><FLogTime task={modal.task} employees={employees} onDone={addTimeEntry} onX={()=>setModal(null)}/></Mod>}
    {modal?.type==="apply_template"&&<Mod title="Aplicar plantilla de tareas" onClose={()=>setModal(null)}><FApplyTemplate templates={templates} targetType={modal.targetType} targetId={modal.targetId} onApply={applyTemplate} onX={()=>setModal(null)}/></Mod>}
    {modal==="upload_pf"&&sp&&<Mod title="Subir Archivo" onClose={()=>setModal(null)}><FU types={PROSP_TYPES} onDone={(f,t)=>uploadFile(f,"prospect",sp.id,t)} onX={()=>setModal(null)}/></Mod>}
    {modal==="upload_del"&&sc&&<Mod title="Subir Entregable" onClose={()=>setModal(null)}><FU types={DEL_TYPES} onDone={(f,t)=>uploadFile(f,"client",sc.id,t)} onX={()=>setModal(null)}/></Mod>}
    {modal?.type==="edit_user"&&<Mod title="Editar Usuario" onClose={()=>setModal(null)}><FEditUser user={modal.user} table={modal.table} onDone={async upd=>{await dbUpd(modal.table,modal.user.id,upd);setModal(null)}} onX={()=>setModal(null)}/></Mod>}
    {modal?.type==="onboarding"&&<Mod title="Onboarding — Nuevo Cliente" onClose={()=>setModal(null)} w={640}><FOnboarding prospect={modal.prospect} onDone={(id,data)=>convertProspect(id,data)} onX={()=>setModal(null)}/></Mod>}
    {modal?.type==="dup_period"&&<Mod title="Duplicar periodo de tareas" onClose={()=>setModal(null)} w={560}><FDupPeriod todos={todos} clients={clients} prospects={prospects} onDuplicate={dupPeriod} onX={()=>setModal(null)}/></Mod>}

    {/* COMMAND PALETTE (Cmd+K) */}
    {cmdOpen&&user?.role==="master"&&<div style={{position:"fixed",inset:0,background:"rgba(0,0,0,.6)",zIndex:3000,display:"flex",alignItems:"flex-start",justifyContent:"center",paddingTop:"15vh",backdropFilter:"blur(8px)"}} onClick={e=>{if(e.target===e.currentTarget)setCmdOpen(false)}}>
      <div style={{width:"92%",maxWidth:560,background:`linear-gradient(145deg,${C.s},${C.bg})`,borderRadius:16,border:`1px solid ${C.acc}30`,boxShadow:`0 20px 60px rgba(0,0,0,.5),0 0 30px ${C.acc}10`,animation:"modalIn .2s ease",overflow:"hidden"}}>
        <div style={{display:"flex",alignItems:"center",gap:8,padding:"12px 16px",borderBottom:`1px solid ${C.b}`}}>
          <span style={{color:C.acc,fontSize:16}}>⌘</span>
          <input value={cmdQuery} onChange={e=>setCmdQuery(e.target.value)} autoFocus placeholder="Buscar clientes, tareas, acciones..." style={{flex:1,background:"transparent",border:"none",color:C.tx,fontSize:15,fontFamily:F,outline:"none"}}/>
          <span style={{fontSize:10,color:C.td,padding:"2px 6px",border:`1px solid ${C.b}`,borderRadius:4}}>ESC</span>
        </div>
        <div style={{maxHeight:360,overflow:"auto",padding:8}}>
          {(()=>{const q=cmdQuery.toLowerCase();const results=[];
            if(!q||q.length<1){results.push({type:"action",icon:"📋",label:"Crear nueva tarea",fn:()=>{setCmdOpen(false);setModal({type:"add_todo_global",defaults:{}})}});results.push({type:"action",icon:"🎯",label:"Nuevo prospecto",fn:()=>{setCmdOpen(false);setModal("add_prospect")}});results.push({type:"action",icon:"💼",label:"Nuevo cliente",fn:()=>{setCmdOpen(false);setModal("add_client")}});results.push({type:"nav",icon:"📊",label:"Ir al Dashboard",fn:()=>{setCmdOpen(false);setView("dashboard")}});results.push({type:"nav",icon:"📅",label:"Ir al Cronograma",fn:()=>{setCmdOpen(false);setView("timeline")}});results.push({type:"nav",icon:"💸",label:"Ir a Gastos",fn:()=>{setCmdOpen(false);setView("gastos")}});results.push({type:"nav",icon:"🔀",label:"Ir al Pipeline",fn:()=>{setCmdOpen(false);setView("pipeline")}});
            }else{
              clients.filter(c=>(c.company+c.name+(c.email||"")).toLowerCase().includes(q)).slice(0,4).forEach(c=>results.push({type:"client",icon:"💼",label:c.company||c.name,sub:c.email,fn:()=>{setCmdOpen(false);setView("clients");setSelId(c.id)}}));
              prospects.filter(p=>(p.company+p.name+(p.email||"")).toLowerCase().includes(q)).slice(0,3).forEach(p=>results.push({type:"prospect",icon:"🎯",label:p.company||p.name,sub:p.email,fn:()=>{setCmdOpen(false);setView("prospects");setSelId(p.id)}}));
              todos.filter(t=>(t.title+(t.description||"")).toLowerCase().includes(q)).slice(0,4).forEach(t=>results.push({type:"task",icon:"📋",label:t.title,sub:t.status,fn:()=>{setCmdOpen(false);setView(t.client_id?"clients":"timeline");if(t.client_id)setSelId(t.client_id)}}));
              employees.filter(e=>(e.name+(e.email||"")).toLowerCase().includes(q)).slice(0,2).forEach(e=>results.push({type:"employee",icon:"👤",label:e.name,sub:e.role,fn:()=>{setCmdOpen(false);setView("employees")}}));
              if("tarea task crear nueva".includes(q))results.push({type:"action",icon:"➕",label:"Crear nueva tarea",fn:()=>{setCmdOpen(false);setModal({type:"add_todo_global",defaults:{}})}});
              if("prospecto lead nuevo".includes(q))results.push({type:"action",icon:"➕",label:"Nuevo prospecto",fn:()=>{setCmdOpen(false);setModal("add_prospect")}});
              if("reporte financiero descargar".includes(q))results.push({type:"action",icon:"📥",label:"Descargar reporte financiero",fn:()=>{setCmdOpen(false);document.querySelector("[data-download-finance]")?.click()}});
            }
            if(!results.length)return<div style={{padding:20,textAlign:"center",color:C.td,fontSize:13}}>Sin resultados para "{cmdQuery}"</div>;
            const grouped={};results.forEach(r=>{const g=r.type==="action"?"Acciones":r.type==="nav"?"Navegación":r.type==="client"?"Clientes":r.type==="prospect"?"Prospectos":r.type==="task"?"Tareas":"Equipo";if(!grouped[g])grouped[g]=[];grouped[g].push(r)});
            return Object.entries(grouped).map(([group,items])=><div key={group}>
              <div style={{fontSize:10,fontWeight:700,color:C.td,padding:"8px 10px 4px",textTransform:"uppercase",letterSpacing:".06em"}}>{group}</div>
              {items.map((r,i)=><div key={i} onClick={r.fn} style={{display:"flex",alignItems:"center",gap:10,padding:"8px 10px",borderRadius:8,cursor:"pointer",transition:"all .15s"}} onMouseEnter={e=>e.currentTarget.style.background=C.s2} onMouseLeave={e=>e.currentTarget.style.background="transparent"}>
                <span style={{fontSize:16,flexShrink:0}}>{r.icon}</span>
                <div style={{flex:1}}><div style={{fontSize:13,fontWeight:500,color:C.tx}}>{r.label}</div>{r.sub&&<div style={{fontSize:10,color:C.td}}>{r.sub}</div>}</div>
                <span style={{fontSize:10,color:C.td}}>{r.type==="action"?"↵":r.type==="nav"?"→":"↗"}</span>
              </div>)}
            </div>);
          })()}
        </div>
        <div style={{padding:"8px 16px",borderTop:`1px solid ${C.b}`,display:"flex",gap:12,justifyContent:"center"}}>
          {[{k:"↵",l:"Abrir"},{k:"↑↓",l:"Navegar"},{k:"esc",l:"Cerrar"}].map(h=><span key={h.k} style={{fontSize:10,color:C.td}}><span style={{padding:"1px 4px",border:`1px solid ${C.b}`,borderRadius:3,marginRight:3,fontSize:9}}>{h.k}</span>{h.l}</span>)}
        </div>
      </div>
    </div>}

    {/* QUICK ACTIONS FAB */}
    {user?.role==="master"&&<div style={{position:"fixed",bottom:24,right:24,zIndex:1500}}>
      {fabOpen&&<div style={{position:"absolute",bottom:56,right:0,display:"flex",flexDirection:"column",gap:6,animation:"fadeUp .2s ease"}}>
        {[
          {icon:"📋",label:"Nueva tarea",fn:()=>{setFabOpen(false);setModal({type:"add_todo_global",defaults:{}})}},
          {icon:"🎯",label:"Nuevo prospecto",fn:()=>{setFabOpen(false);setModal("add_prospect")}},
          {icon:"💼",label:"Nuevo cliente",fn:()=>{setFabOpen(false);setModal("add_client")}},
          {icon:"⌘K",label:"Buscar",fn:()=>{setFabOpen(false);setCmdOpen(true);setCmdQuery("")}},
        ].map((a,i)=><div key={i} onClick={a.fn} style={{display:"flex",alignItems:"center",gap:8,padding:"8px 14px",background:`linear-gradient(145deg,${C.s},${C.bg})`,border:`1px solid ${C.b}`,borderRadius:10,cursor:"pointer",whiteSpace:"nowrap",backdropFilter:"blur(12px)",boxShadow:"0 4px 15px rgba(0,0,0,.3)"}} onMouseEnter={e=>e.currentTarget.style.borderColor=C.acc} onMouseLeave={e=>e.currentTarget.style.borderColor=C.b}>
          <span style={{fontSize:14}}>{a.icon}</span><span style={{fontSize:12,fontWeight:500,color:C.tx,fontFamily:F}}>{a.label}</span>
        </div>)}
      </div>}
      <button onClick={()=>setFabOpen(!fabOpen)} style={{width:48,height:48,borderRadius:14,background:`linear-gradient(135deg,${C.acc},#D4A00E)`,border:"none",cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:20,boxShadow:`0 6px 20px rgba(248,186,16,.3)`,transition:"all .3s",transform:fabOpen?"rotate(45deg)":""}}>+</button>
    </div>}

  </div>;
}

// ═══ FORMS ═══
function SyncForm({onSync,onCancel,saving,syncStatus}){const[url,setUrl]=useState(localStorage.getItem("wauya_leads_folder")||"");return<div style={{display:"flex",flexDirection:"column",gap:14}}><div style={{background:C.bg,borderRadius:10,padding:14,border:`1px solid ${C.b}`,fontSize:12,color:C.tm,lineHeight:1.7}}>Pega el link de tu carpeta <strong style={{color:C.acc}}>leads entrantes</strong>.</div><Inp label="Link de carpeta" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://drive.google.com/drive/folders/..."/>{syncStatus&&<div style={{fontSize:12,color:C.acc}}>{syncStatus}</div>}<div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onCancel} v="ghost">Cancelar</Btn><Btn onClick={()=>onSync(url)} disabled={!url||saving} icon="sync">{saving?"...":"Sincronizar"}</Btn></div></div>}

function FLogTime({task,employees,onDone,onX}){const[f,s]=useState({employee_id:"",hours:1,date:todayStr(),description:task?.title||""});return<div style={{display:"flex",flexDirection:"column",gap:14}}><div style={{fontSize:13,fontWeight:600,color:C.tx}}>{task?.title}</div><Sel label="Empleado" value={f.employee_id} onChange={e=>s({...f,employee_id:e.target.value})} options={[{value:"",label:"— Seleccionar —"},...employees.map(e=>({value:e.id,label:e.name}))]}/><Inp label="Horas" type="number" step="0.5" min="0.5" max="12" value={f.hours} onChange={e=>s({...f,hours:e.target.value})}/><Inp label="Fecha" type="date" value={f.date} onChange={e=>s({...f,date:e.target.value})}/><Inp label="Descripción" value={f.description} onChange={e=>s({...f,description:e.target.value})}/><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(!f.employee_id){alert("Selecciona empleado");return}onDone({...f,hours:parseFloat(f.hours)||0,todo_id:task?.id||null,client_id:task?.client_id||null})}} disabled={!f.employee_id}>Registrar</Btn></div></div>}

function FApplyTemplate({templates,targetType,targetId,onApply,onX}){const[sel,setSel]=useState("");return<div style={{display:"flex",flexDirection:"column",gap:14}}><div style={{fontSize:12,color:C.tm,lineHeight:1.7}}>Selecciona una plantilla para crear múltiples tareas automáticamente.</div>{templates.length===0?<Empty icon="📋" title="Sin plantillas" sub="Se crean automáticamente al ejecutar el SQL"/>:<div style={{display:"flex",flexDirection:"column",gap:8}}>{templates.map(t=>{const tasks=typeof t.tasks==="string"?JSON.parse(t.tasks):t.tasks;return<div key={t.id} onClick={()=>setSel(t.id)} style={{padding:"12px 14px",background:sel===t.id?C.acc+"15":C.bg,border:`1px solid ${sel===t.id?C.acc:C.b}`,borderRadius:10,cursor:"pointer"}}><div style={{fontSize:13,fontWeight:600,color:C.tx}}>{t.name}</div><div style={{fontSize:11,color:C.td,marginTop:2}}><CatBadge cat={t.category}/> · {tasks.length} tareas</div><div style={{fontSize:10,color:C.tm,marginTop:6}}>{tasks.map(tk=>tk.title).join(" · ")}</div></div>})}</div>}<div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(sel)onApply(sel,targetType,targetId)}} disabled={!sel}>Aplicar plantilla</Btn></div></div>}

function FTGlobal({clients,prospects,emps,onDone,onX,init,isEdit,defaults={}}){const[f,s]=useState(init?{title:init.title||"",description:init.description||"",priority:init.priority||"media",status:init.status||"pendiente",assigned_to:init.assigned_to||"",assigned_to_2:init.assigned_to_2||"",start_date:init.start_date||todayStr(),end_date:init.end_date||"",category:init.category||"cliente",client_id:init.client_id||"",prospect_id:init.prospect_id||"",url:init.url||"",project_type:init.project_type||""}:{title:"",description:"",priority:defaults.priority||"alta",status:"pendiente",assigned_to:"",assigned_to_2:"",start_date:todayStr(),end_date:"",category:defaults.category||"cliente",client_id:defaults.client_id||"",prospect_id:defaults.prospect_id||"",url:"",project_type:""});return<div style={{display:"flex",flexDirection:"column",gap:14}}><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Sel label="Categoría" value={f.category} onChange={e=>s({...f,category:e.target.value,client_id:e.target.value!=="cliente"?null:f.client_id,prospect_id:e.target.value!=="propuesta"?null:f.prospect_id})} options={CATS}/><Sel label="Tipo de proyecto" value={f.project_type} onChange={e=>s({...f,project_type:e.target.value})} options={PROJ_TYPES}/></div>{f.category==="cliente"&&<Sel label="Cliente" value={f.client_id||""} onChange={e=>s({...f,client_id:e.target.value||null})} options={[{value:"",label:"— Seleccionar —"},...clients.map(c=>({value:c.id,label:c.company||c.name}))]}/>}{f.category==="propuesta"&&<Sel label="Prospecto" value={f.prospect_id||""} onChange={e=>s({...f,prospect_id:e.target.value||null})} options={[{value:"",label:"— Seleccionar —"},...prospects.map(p=>({value:p.id,label:p.company||p.name}))]}/>}<Inp label="Título" value={f.title} onChange={e=>s({...f,title:e.target.value})} placeholder="Diseñar propuesta visual..."/><Txt label="Detalles" value={f.description} onChange={e=>s({...f,description:e.target.value})}/><Inp label="URL de referencia" value={f.url} onChange={e=>s({...f,url:e.target.value})} placeholder="https://..."/><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Sel label="Prioridad" value={f.priority} onChange={e=>s({...f,priority:e.target.value})} options={PRIOS}/><Sel label="Estado" value={f.status} onChange={e=>s({...f,status:e.target.value})} options={TSTAT}/></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Sel label="Asignar a (principal)" value={f.assigned_to||""} onChange={e=>s({...f,assigned_to:e.target.value||null})} options={[{value:"",label:"Sin asignar"},...emps.map(e=>({value:e.id,label:e.name}))]}/><Sel label="Asignar a (apoyo)" value={f.assigned_to_2||""} onChange={e=>s({...f,assigned_to_2:e.target.value||null})} options={[{value:"",label:"— Ninguno —"},...emps.map(e=>({value:e.id,label:e.name}))]}/></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Inp label="Inicio" type="date" value={f.start_date||""} onChange={e=>s({...f,start_date:e.target.value})}/><Inp label="Entrega" type="date" value={f.end_date||""} onChange={e=>s({...f,end_date:e.target.value})}/></div><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(f.title){const d={...f};if(!d.client_id)d.client_id=null;if(!d.prospect_id)d.prospect_id=null;if(!d.assigned_to)d.assigned_to=null;if(!d.assigned_to_2)d.assigned_to_2=null;onDone(d)}}} disabled={!f.title}>{isEdit?"Guardar":"Crear"}</Btn></div></div>}
function FP({onDone,onX}){const[f,s]=useState({name:"",company:"",email:"",phone:"",services:""});return<div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Nombre" value={f.name} onChange={e=>s({...f,name:e.target.value})}/><Inp label="Empresa" value={f.company} onChange={e=>s({...f,company:e.target.value})}/><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Inp label="Email" type="email" value={f.email} onChange={e=>s({...f,email:e.target.value})}/><Inp label="Teléfono" value={f.phone} onChange={e=>s({...f,phone:e.target.value})}/></div><Inp label="Servicios" value={f.services} onChange={e=>s({...f,services:e.target.value})}/><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(f.name)onDone(f)}} disabled={!f.name}>Crear</Btn></div></div>}
function FC({onDone,onX}){const[f,s]=useState({name:"",company:"",email:"",phone:"",services:"",notes:""});return<div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Nombre" value={f.name} onChange={e=>s({...f,name:e.target.value})}/><Inp label="Empresa" value={f.company} onChange={e=>s({...f,company:e.target.value})}/><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><Inp label="Email" type="email" value={f.email} onChange={e=>s({...f,email:e.target.value})}/><Inp label="Teléfono" value={f.phone} onChange={e=>s({...f,phone:e.target.value})}/></div><Inp label="Servicios" value={f.services} onChange={e=>s({...f,services:e.target.value})}/><Txt label="Notas" value={f.notes} onChange={e=>s({...f,notes:e.target.value})}/><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(f.name)onDone(f)}} disabled={!f.name}>Crear</Btn></div></div>}
function FCU({onDone,onX}){const[f,s]=useState({name:"",email:"",password:""});const gen=()=>{let p="";for(let i=0;i<8;i++)p+="abcdefghijkmnpqrstuvwxyz23456789"[Math.floor(Math.random()*31)];s({...f,password:p})};return<div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Nombre" value={f.name} onChange={e=>s({...f,name:e.target.value})}/><Inp label="Email" type="email" value={f.email} onChange={e=>s({...f,email:e.target.value})}/><div><Inp label="Contraseña" value={f.password} onChange={e=>s({...f,password:e.target.value})}/><button onClick={gen} style={{background:"none",border:"none",color:C.acc,fontSize:10,cursor:"pointer",fontFamily:F,marginTop:4}}>Generar</button></div><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(f.name&&f.email&&f.password)onDone(f)}} disabled={!f.name||!f.email||!f.password}>Crear</Btn></div></div>}
function FE({onDone,onX}){const[f,s]=useState({name:"",email:"",password:"",role:"",hourly_rate:0});const gen=()=>{let p="";for(let i=0;i<8;i++)p+="abcdefghijkmnpqrstuvwxyz23456789"[Math.floor(Math.random()*31)];s({...f,password:p})};return<div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Nombre" value={f.name} onChange={e=>s({...f,name:e.target.value})}/><Inp label="Cargo" value={f.role} onChange={e=>s({...f,role:e.target.value})}/><Inp label="Email" type="email" value={f.email} onChange={e=>s({...f,email:e.target.value})}/><div><Inp label="Contraseña" value={f.password} onChange={e=>s({...f,password:e.target.value})}/><button onClick={gen} style={{background:"none",border:"none",color:C.acc,fontSize:10,cursor:"pointer",fontFamily:F,marginTop:4}}>Generar</button></div><Inp label="Rate por hora ($)" type="number" value={f.hourly_rate} onChange={e=>s({...f,hourly_rate:e.target.value})}/><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(f.name&&f.email&&f.password)onDone({...f,hourly_rate:parseFloat(f.hourly_rate)||0})}} disabled={!f.name||!f.email||!f.password}>Crear</Btn></div></div>}
function FU({types,onDone,onX}){const[tp,sTp]=useState(types[0].value);const[sel,sSel]=useState(null);const ref=useRef(null);return<div style={{display:"flex",flexDirection:"column",gap:14}}><Sel label="Tipo" value={tp} onChange={e=>sTp(e.target.value)} options={types}/><div onClick={()=>ref.current?.click()} style={{border:`2px dashed ${C.b}`,borderRadius:12,padding:28,textAlign:"center",cursor:"pointer"}}><input ref={ref} type="file" style={{display:"none"}} onChange={e=>{if(e.target.files[0])sSel(e.target.files[0])}}/>{sel?<div style={{fontSize:12,color:C.tx}}>📎 {sel.name}</div>:<div><div style={{fontSize:24,marginBottom:6}}>📁</div><div style={{fontSize:12,color:C.tm}}>Click para seleccionar</div></div>}</div><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(sel)onDone(sel,tp)}} disabled={!sel}>Subir</Btn></div></div>}
function FEditUser({user:u,table,onDone,onX}){const[f,s]=useState({name:u.name||"",email:u.email||"",password:u.password||""});const[showPw,setShowPw]=useState(false);const gen=()=>{let p="";for(let i=0;i<8;i++)p+="abcdefghijkmnpqrstuvwxyz23456789"[Math.floor(Math.random()*31)];s({...f,password:p})};return<div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Nombre" value={f.name} onChange={e=>s({...f,name:e.target.value})}/><Inp label="Email" type="email" value={f.email} onChange={e=>s({...f,email:e.target.value})}/><div><Inp label="Contraseña" type={showPw?"text":"password"} value={f.password} onChange={e=>s({...f,password:e.target.value})}/><div style={{display:"flex",gap:12,marginTop:4}}><button onClick={()=>setShowPw(!showPw)} style={{background:"none",border:"none",color:C.tm,fontSize:10,cursor:"pointer",fontFamily:F}}>{showPw?"🙈":"👁️"}</button><button onClick={gen} style={{background:"none",border:"none",color:C.acc,fontSize:10,cursor:"pointer",fontFamily:F}}>🔄 Generar</button></div></div><div style={{display:"flex",gap:8,justifyContent:"flex-end"}}><Btn onClick={onX} v="ghost">Cancelar</Btn><Btn onClick={()=>{if(f.name&&f.email&&f.password)onDone(f)}} disabled={!f.name||!f.email||!f.password}>Guardar</Btn></div></div>}
function SettingsView({user:u,onSave,onBack}){const[name,setName]=useState(u?.name||"");const[email,setEmail]=useState(u?.email||"");const[curPw,setCurPw]=useState("");const[newPw,setNewPw]=useState("");const[confirmPw,setConfirmPw]=useState("");const[showPw,setShowPw]=useState(false);const[msg,setMsg]=useState("");const[err,setErr]=useState("");const handleSave=async()=>{setErr("");setMsg("");const{data}=await supabase.from("admin_users").select("password").eq("id",u.id).single();if(!data||data.password!==curPw){setErr("Contraseña incorrecta");return}const upd={name,email};if(newPw){if(newPw.length<4){setErr("Mínimo 4 caracteres");return}if(newPw!==confirmPw){setErr("No coinciden");return}upd.password=newPw}await onSave(upd);setCurPw("");setNewPw("");setConfirmPw("");setMsg("Actualizado")};return<div><button onClick={onBack} style={{display:"flex",alignItems:"center",gap:5,background:"none",border:"none",color:C.tm,cursor:"pointer",fontSize:12,marginBottom:18,padding:0,fontFamily:F}}><Ic n="back" sz={15}/>Volver</button><h1 style={{fontFamily:D,fontSize:24,fontWeight:700,color:C.tx,marginBottom:24}}>Configuración</h1><Card style={{marginBottom:20,maxWidth:500}}><h3 style={{fontFamily:D,fontSize:14,fontWeight:600,color:C.tx,marginBottom:16}}>Cuenta</h3><div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Nombre" value={name} onChange={e=>setName(e.target.value)}/><Inp label="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div></Card><Card style={{marginBottom:20,maxWidth:500}}><h3 style={{fontFamily:D,fontSize:14,fontWeight:600,color:C.tx,marginBottom:16}}>Contraseña</h3><div style={{display:"flex",flexDirection:"column",gap:14}}><Inp label="Actual" type={showPw?"text":"password"} value={curPw} onChange={e=>setCurPw(e.target.value)}/><Inp label="Nueva" type={showPw?"text":"password"} value={newPw} onChange={e=>setNewPw(e.target.value)}/><Inp label="Confirmar" type={showPw?"text":"password"} value={confirmPw} onChange={e=>setConfirmPw(e.target.value)}/><button onClick={()=>setShowPw(!showPw)} style={{background:"none",border:"none",color:C.tm,fontSize:11,cursor:"pointer",fontFamily:F,textAlign:"left",padding:0}}>{showPw?"🙈 Ocultar":"👁️ Mostrar"}</button></div></Card>{err&&<div style={{color:C.r,fontSize:12,marginBottom:12}}>❌ {err}</div>}{msg&&<div style={{color:C.g,fontSize:12,marginBottom:12}}>✅ {msg}</div>}<Btn onClick={handleSave} disabled={!curPw||!name||!email} sz="lg">Guardar</Btn></div>}

// ─── ONBOARDING WIZARD ───
function FOnboarding({prospect,onDone,onX}){
  const[step,setStep]=useState(1);
  const[f,s]=useState({services:prospect?.services||"",notes:prospect?.notes||"",target_audience:"",brand_colors:"",social_instagram:"",social_facebook:"",social_tiktok:"",website:"",goals:"",monthly_fee:0,billing_day:1});
  const steps=[{n:1,t:"Servicios",icon:"🎯"},{n:2,t:"Audiencia",icon:"👥"},{n:3,t:"Marca",icon:"🎨"},{n:4,t:"Objetivos",icon:"🏆"}];
  return<div>
    {/* Progress */}
    <div style={{display:"flex",gap:4,marginBottom:20}}>{steps.map(st=><div key={st.n} style={{flex:1,textAlign:"center"}}><div style={{height:4,borderRadius:2,background:step>=st.n?C.acc:C.b,marginBottom:4,transition:"all .3s"}}/><div style={{fontSize:9,color:step>=st.n?C.acc:C.td}}>{st.icon} {st.t}</div></div>)}</div>
    <div style={{fontSize:13,fontWeight:600,color:C.tx,marginBottom:4,fontFamily:D}}>Onboarding: {prospect?.company||prospect?.name}</div>
    <div style={{fontSize:11,color:C.tm,marginBottom:16}}>Paso {step} de 4 — {steps[step-1]?.t}</div>

    {step===1&&<div style={{display:"flex",flexDirection:"column",gap:14}}>
      <Txt label="Servicios contratados" value={f.services} onChange={e=>s({...f,services:e.target.value})} placeholder="Social Media, Branding, Ads..."/>
      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>
        <Inp label="Fee mensual ($)" type="number" value={f.monthly_fee} onChange={e=>s({...f,monthly_fee:e.target.value})}/>
        <Inp label="Día de cobro" type="number" min="1" max="31" value={f.billing_day} onChange={e=>s({...f,billing_day:e.target.value})}/>
      </div>
    </div>}

    {step===2&&<div style={{display:"flex",flexDirection:"column",gap:14}}>
      <Txt label="¿Quién es su público objetivo?" value={f.target_audience} onChange={e=>s({...f,target_audience:e.target.value})} placeholder="Mujeres 25-45 años interesadas en..."/>
      <Inp label="Sitio web" value={f.website} onChange={e=>s({...f,website:e.target.value})} placeholder="https://..."/>
    </div>}

    {step===3&&<div style={{display:"flex",flexDirection:"column",gap:14}}>
      <Inp label="Colores de marca (hex o descripción)" value={f.brand_colors} onChange={e=>s({...f,brand_colors:e.target.value})} placeholder="#012762, dorado, negro..."/>
      <Inp label="Instagram" value={f.social_instagram} onChange={e=>s({...f,social_instagram:e.target.value})} placeholder="@usuario"/>
      <Inp label="Facebook" value={f.social_facebook} onChange={e=>s({...f,social_facebook:e.target.value})} placeholder="facebook.com/pagina"/>
      <Inp label="TikTok" value={f.social_tiktok} onChange={e=>s({...f,social_tiktok:e.target.value})} placeholder="@usuario"/>
    </div>}

    {step===4&&<div style={{display:"flex",flexDirection:"column",gap:14}}>
      <Txt label="Objetivos principales del cliente" value={f.goals} onChange={e=>s({...f,goals:e.target.value})} placeholder="Aumentar seguidores, generar leads, más ventas..."/>
      <Txt label="Notas adicionales" value={f.notes} onChange={e=>s({...f,notes:e.target.value})} placeholder="Cualquier detalle importante..."/>
      {/* Summary */}
      <div style={{background:C.bg,borderRadius:10,padding:14,border:`1px solid ${C.b}`}}>
        <div style={{fontSize:11,fontWeight:600,color:C.acc,marginBottom:8}}>Resumen del onboarding:</div>
        <div style={{fontSize:10,color:C.tm,lineHeight:1.8}}>
          {f.services&&<div>🎯 <strong>Servicios:</strong> {f.services}</div>}
          {f.monthly_fee>0&&<div>💰 <strong>Fee:</strong> ${f.monthly_fee}/mes (día {f.billing_day})</div>}
          {f.target_audience&&<div>👥 <strong>Audiencia:</strong> {f.target_audience}</div>}
          {f.website&&<div>🌐 <strong>Web:</strong> {f.website}</div>}
          {f.brand_colors&&<div>🎨 <strong>Colores:</strong> {f.brand_colors}</div>}
          {(f.social_instagram||f.social_facebook||f.social_tiktok)&&<div>📱 <strong>Redes:</strong> {[f.social_instagram,f.social_facebook,f.social_tiktok].filter(Boolean).join(" · ")}</div>}
          {f.goals&&<div>🏆 <strong>Objetivos:</strong> {f.goals}</div>}
        </div>
      </div>
    </div>}

    <div style={{display:"flex",justifyContent:"space-between",marginTop:20}}>
      {step>1?<Btn onClick={()=>setStep(step-1)} v="ghost">← Anterior</Btn>:<Btn onClick={onX} v="ghost">Cancelar</Btn>}
      {step<4?<Btn onClick={()=>setStep(step+1)}>Siguiente →</Btn>:
        <Btn onClick={()=>{
          const notes=[f.notes,f.target_audience&&"👥 Audiencia: "+f.target_audience,f.brand_colors&&"🎨 Colores: "+f.brand_colors,(f.social_instagram||f.social_facebook||f.social_tiktok)&&"📱 Redes: "+[f.social_instagram,f.social_facebook,f.social_tiktok].filter(Boolean).join(", "),f.website&&"🌐 Web: "+f.website,f.goals&&"🏆 Objetivos: "+f.goals].filter(Boolean).join("\n");
          onDone(prospect.id,{services:f.services,notes,monthly_fee:parseFloat(f.monthly_fee)||0,billing_day:parseInt(f.billing_day)||1})
        }}>✅ Crear cliente</Btn>}
    </div>
  </div>;
}

// ─── DUPLICATE PERIOD FORM ───
function FDupPeriod({todos,clients,prospects,onDuplicate,onX}){
  const[clientFilter,setCF]=useState("all");
  const[dateFrom,setDF]=useState("");
  const[dateTo,setDT]=useState("");
  const[dayShift,setDS]=useState(7);
  const[preview,setPV]=useState([]);

  const buildPreview=()=>{
    let filtered=todos.filter(t=>t.status!=="completado"&&t.start_date);
    if(clientFilter!=="all"){
      if(clientFilter==="interno")filtered=filtered.filter(t=>!t.client_id&&!t.prospect_id);
      else filtered=filtered.filter(t=>t.client_id===clientFilter||t.prospect_id===clientFilter);
    }
    if(dateFrom)filtered=filtered.filter(t=>t.start_date>=dateFrom);
    if(dateTo)filtered=filtered.filter(t=>t.start_date<=dateTo);
    setPV(filtered);
  };

  const allEntities=[
    {value:"all",label:"— Todos —"},
    ...clients.map(c=>({value:c.id,label:"💼 "+(c.company||c.name)})),
    ...prospects.map(p=>({value:p.id,label:"📄 "+(p.company||p.name)})),
    {value:"interno",label:"🏠 Interno"},
  ];

  return<div style={{display:"flex",flexDirection:"column",gap:14}}>
    <div style={{background:C.bg,borderRadius:10,padding:14,border:`1px solid ${C.b}`,fontSize:12,color:C.tm,lineHeight:1.7}}>
      Selecciona un rango de fechas y las tareas se duplicarán con las fechas desplazadas. Ideal para copiar <strong style={{color:C.acc}}>semana 1 → semana 2</strong> o <strong style={{color:C.acc}}>mes 1 → mes 2</strong>.
    </div>
    <Sel label="Cliente / Prospecto" value={clientFilter} onChange={e=>setCF(e.target.value)} options={allEntities}/>
    <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>
      <Inp label="Desde" type="date" value={dateFrom} onChange={e=>setDF(e.target.value)}/>
      <Inp label="Hasta" type="date" value={dateTo} onChange={e=>setDT(e.target.value)}/>
    </div>
    <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>
      <Sel label="Desplazar" value={dayShift} onChange={e=>setDS(parseInt(e.target.value))} options={[{value:7,label:"+7 días (1 semana)"},{value:14,label:"+14 días (2 semanas)"},{value:30,label:"+30 días (1 mes)"},{value:60,label:"+60 días (2 meses)"}]}/>
      <div style={{display:"flex",alignItems:"flex-end"}}><Btn onClick={buildPreview} v="secondary" style={{width:"100%",justifyContent:"center"}}>🔍 Previsualizar</Btn></div>
    </div>

    {preview.length>0&&<div>
      <div style={{fontSize:11,fontWeight:600,color:C.acc,marginBottom:6}}>{preview.length} tareas a duplicar:</div>
      <div style={{maxHeight:200,overflow:"auto",display:"flex",flexDirection:"column",gap:3}}>
        {preview.map((t,i)=>{const cl=clients.find(c=>c.id===t.client_id);return<div key={i} style={{fontSize:11,padding:"4px 8px",background:C.bg,borderRadius:6,border:`1px solid ${C.b}`,display:"flex",justifyContent:"space-between"}}>
          <span style={{color:C.tx}}>{t.title}</span>
          <span style={{color:C.td,fontSize:9}}>{t.start_date} → {t.start_date?(() => {const d=new Date(t.start_date);d.setDate(d.getDate()+parseInt(dayShift));return d.toISOString().split("T")[0]})():""}{cl?" · "+cl.company:""}</span>
        </div>})}
      </div>
    </div>}

    <div style={{display:"flex",gap:8,justifyContent:"flex-end"}}>
      <Btn onClick={onX} v="ghost">Cancelar</Btn>
      <Btn onClick={()=>{if(!preview.length){alert("Primero dale Previsualizar");return}onDuplicate(preview,parseInt(dayShift));onX()}} disabled={!preview.length}>📋 Duplicar {preview.length} tareas</Btn>
    </div>
  </div>;
}

// ─── SERVICE FORM (mensual / único / mixto) ───


// ─── CLIENT CREDENTIALS (admin: view + add + edit + delete) ───
function ClientCreds({clientId}){
  const[creds,setCreds]=useState([]);const[showPw,setShowPw]=useState({});const[editing,setEditing]=useState(null);
  const[addForm,setAddForm]=useState(null);
  const PLATS=[{value:"instagram",label:"Instagram",icon:"📸"},{value:"facebook",label:"Facebook",icon:"📘"},{value:"tiktok",label:"TikTok",icon:"🎵"},{value:"linkedin",label:"LinkedIn",icon:"💼"},{value:"twitter",label:"Twitter/X",icon:"🐦"},{value:"youtube",label:"YouTube",icon:"▶️"},{value:"google_business",label:"Google Business",icon:"📍"},{value:"website",label:"Website",icon:"🌐"},{value:"email_marketing",label:"Email marketing",icon:"📧"},{value:"hosting",label:"Hosting",icon:"🖥️"},{value:"otro",label:"Otro",icon:"🔑"}];
  const reload=async()=>{try{const{data}=await supabase.from("client_credentials").select("*").eq("client_id",clientId).order("created_at");if(data)setCreds(data)}catch{}};
  useEffect(()=>{if(clientId)reload()},[clientId]);

  const saveCred=async(form)=>{try{if(!form.username&&!form.password)return;await supabase.from("client_credentials").insert({client_id:clientId,...form});await reload();setAddForm(null)}catch{}};
  const updateCred=async(id,upd)=>{try{await supabase.from("client_credentials").update(upd).eq("id",id);await reload();setEditing(null)}catch{}};
  const deleteCred=async(id)=>{try{if(!confirm("¿Eliminar credencial?"))return;await supabase.from("client_credentials").delete().eq("id",id);await reload()}catch{}};

  return<div>
    <div style={{display:"flex",justifyContent:"flex-end",marginBottom:8}}><button onClick={()=>setAddForm(addForm?null:{platform:"instagram",username:"",password:"",url:""})} style={{background:addForm?"transparent":`linear-gradient(135deg,${C.acc},#D4A00E)`,color:addForm?C.tm:"#060B18",border:`1px solid ${addForm?C.b:C.acc}`,borderRadius:8,padding:"5px 12px",fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:F}}>{addForm?"✕ Cancelar":"+ Agregar credencial"}</button></div>

    {addForm&&<div style={{background:C.bg,borderRadius:10,padding:14,border:`1px solid ${C.acc}30`,marginBottom:12}}>
      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8,marginBottom:8}}>
        <Sel value={addForm.platform} onChange={e=>setAddForm({...addForm,platform:e.target.value})} options={PLATS}/>
        <Inp value={addForm.username} onChange={e=>setAddForm({...addForm,username:e.target.value})} placeholder="Usuario / email"/>
      </div>
      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8,marginBottom:8}}>
        <Inp value={addForm.password} onChange={e=>setAddForm({...addForm,password:e.target.value})} placeholder="Contraseña"/>
        <Inp value={addForm.url} onChange={e=>setAddForm({...addForm,url:e.target.value})} placeholder="URL (opcional)"/>
      </div>
      <Btn onClick={()=>saveCred(addForm)} sz="sm">Guardar</Btn>
    </div>}

    {creds.length===0&&!addForm&&<p style={{fontSize:11,color:C.td}}>Sin credenciales aún. El cliente las puede ingresar desde su portal, o agrégalas tú.</p>}

    <div style={{display:"flex",flexDirection:"column",gap:6}}>{creds.map(cr=>{
      const pl=PLATS.find(p=>p.value===cr.platform)||{icon:"🔑",label:cr.platform};
      const isEdit=editing===cr.id;

      if(isEdit)return<div key={cr.id} style={{padding:"10px 12px",background:C.bg,borderRadius:10,border:`1px solid ${C.acc}30`}}>
        <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8,marginBottom:8}}>
          <Sel value={cr.platform} onChange={e=>{setCreds(creds.map(c=>c.id===cr.id?{...c,platform:e.target.value}:c))}} options={PLATS}/>
          <Inp value={cr.username} onChange={e=>{setCreds(creds.map(c=>c.id===cr.id?{...c,username:e.target.value}:c))}} placeholder="Usuario"/>
        </div>
        <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8,marginBottom:8}}>
          <Inp value={cr.password} onChange={e=>{setCreds(creds.map(c=>c.id===cr.id?{...c,password:e.target.value}:c))}} placeholder="Contraseña"/>
          <Inp value={cr.url||""} onChange={e=>{setCreds(creds.map(c=>c.id===cr.id?{...c,url:e.target.value}:c))}} placeholder="URL"/>
        </div>
        <div style={{display:"flex",gap:6}}><Btn onClick={()=>updateCred(cr.id,{platform:cr.platform,username:cr.username,password:cr.password,url:cr.url})} sz="sm">Guardar</Btn><Btn onClick={()=>{setEditing(null);reload()}} v="ghost" sz="sm">Cancelar</Btn></div>
      </div>;

      return<div key={cr.id} style={{display:"flex",alignItems:"center",gap:8,padding:"8px 10px",background:C.bg,borderRadius:8,border:`1px solid ${C.b}`}}>
        <span style={{fontSize:14}}>{pl.icon}</span>
        <div style={{flex:1,minWidth:0}}>
          <div style={{fontSize:12,fontWeight:500,color:C.tx}}>{pl.label} {cr.username?`— ${cr.username}`:""}</div>
          {cr.url&&<div style={{fontSize:10,color:C.bl}}>🔗 {cr.url}</div>}
        </div>
        <button onClick={()=>setShowPw({...showPw,[cr.id]:!showPw[cr.id]})} style={{background:C.s2,border:`1px solid ${C.b}`,borderRadius:6,padding:"4px 8px",color:C.tm,cursor:"pointer",fontSize:10,fontFamily:F}}>{showPw[cr.id]?cr.password:"🔒 Ver"}</button>
        <button onClick={()=>{navigator.clipboard.writeText(cr.password||"")}} style={{background:"none",border:"none",cursor:"pointer",color:C.tm,padding:3,fontSize:10}} title="Copiar contraseña">📋</button>
        <button onClick={()=>setEditing(cr.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.bl,padding:3,fontSize:10}} title="Editar">✏️</button>
        <button onClick={()=>deleteCred(cr.id)} style={{background:"none",border:"none",cursor:"pointer",color:C.r,padding:3,fontSize:10}} title="Eliminar">🗑️</button>
      </div>;
    })}</div>
  </div>;
}
